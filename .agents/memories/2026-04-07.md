# Daily Memory Log - 2026-04-07

## Tasks

- [x] Remove authored-agent `mode` from the current contract and CLI creation flow while still reading legacy `mode: safe|yolo` files.
- [x] Normalize Codex and Gemini launches to one write-enabled runtime contract with project writes plus per-run `artifacts/` access.
- [x] Stop appending the runtime artifact-storage footer to rendered prompts and keep only explicit placeholder substitution.
- [x] Remove mode-driven rights/reporting from current run surfaces while keeping old persisted run records readable.
- [x] Update docs, examples, and durable memory to describe the new single-mode contract.

## Current Status

- Current focus: none.
- Last completed action: Removed `mode` from new agent authoring/runtime writes, kept legacy read compatibility for old agent and run records, and updated the docs plus durable memory to describe the provider-based write-enabled launch contract.
- Blocked by: none.

## Progress Log

- 09:10 - Started the mode-removal refactor after confirming that `mode` still affected agent validation, provider launch args, run persistence, CLI/TUI surfaces, and docs.
- 09:20 - Removed `mode` from new agent creation and validation, while still tolerating legacy `mode: safe|yolo` frontmatter on existing agent files.
- 09:28 - Simplified provider launches so Codex always uses `--sandbox workspace-write --add-dir <artifactsDir>` and Gemini always uses `--approval-mode yolo --include-directories <artifactsDir>`.
- 09:35 - Removed the appended runtime artifact-storage prompt footer and kept prompt rendering to explicit placeholder substitution only.
- 09:44 - Updated run persistence, run index bridging, CLI/TUI summaries, and rights reporting so new runs no longer write or depend on `mode`, but older persisted `mode` values still parse.
- 09:52 - Verified with `bun test test/provider-adapters.test.ts`, `bun test test/runs.test.ts`, `bun test test/cli.test.ts`, `bun test test/api.test.ts`, `bun test test/ui-workbench.test.tsx`, and `bun run typecheck`.
- 09:58 - Updated `docs/cli.md`, `docs/agent-runtime.md`, `docs/agent-authoring.md`, example agents, `ARCHITECTURE.md`, and `MEMORY.md` to match the new contract.
