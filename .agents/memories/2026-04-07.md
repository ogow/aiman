# Daily Memory Log - 2026-04-07

## Tasks

- [x] Expand root skill docs with stronger guidance for reliable authored agents, including how to choose boundaries and output shapes.
- [x] Add reusable skill reference material for thinking about agent design and debugging failed or weak `aiman` runs.
- [x] Refresh general docs and examples so agent authoring and debugging guidance matches the root skill docs.
- [x] Remove authored-agent `mode` from the current contract and CLI creation flow while still reading legacy `mode: safe|yolo` files.
- [x] Normalize Codex and Gemini launches to one write-enabled runtime contract with project writes plus per-run `artifacts/` access.
- [x] Stop appending the runtime artifact-storage footer to rendered prompts and keep only explicit placeholder substitution.
- [x] Remove mode-driven rights/reporting from current run surfaces while keeping old persisted run records readable.
- [x] Update docs, examples, and durable memory to describe the new single-mode contract.
- [x] Add one runtime-enforced JSON response contract for all agents in `src/lib/providers/` and `src/lib/runs.ts`.
- [x] Persist canonical structured agent output in `result.json` and surface it through CLI/API run types.
- [x] Update agent templates, docs, and tests to match the required JSON handoff format.
- [x] Replace `run.md` with forward-only `result.json` storage under `~/.aiman/runs/<YYYY-MM-DD>/<timestamp-run-folder>/`.
- [x] Remove SQLite and move run discovery/filtering to filesystem scanning over global run folders.
- [x] Keep `stdout.log` and `stderr.log` as raw debug traces while making `result.json` the only canonical handoff record.

## Current Status

- Current focus: none.
- Last completed action: Expanded the root `aiman` authoring skill docs, added agent-design and debugging references, added a dedicated `docs/agent-debugging.md` guide, and tightened the example agent files around explicit result shapes and stop conditions.
- Blocked by: none.

## Progress Log

- 09:10 - Started the mode-removal refactor after confirming that `mode` still affected agent validation, provider launch args, run persistence, CLI/TUI surfaces, and docs.
- 09:20 - Removed `mode` from new agent creation and validation, while still tolerating legacy `mode: safe|yolo` frontmatter on existing agent files.
- 09:28 - Simplified provider launches so Codex always uses `--sandbox workspace-write --add-dir <artifactsDir>` and Gemini always uses `--approval-mode yolo --include-directories <artifactsDir>`.
- 09:35 - Removed the appended runtime artifact-storage prompt footer and kept prompt rendering to explicit placeholder substitution only.
- 09:44 - Updated run persistence, run index bridging, CLI/TUI summaries, and rights reporting so new runs no longer write or depend on `mode`, but older persisted `mode` values still parse.
- 09:52 - Verified with `bun test test/provider-adapters.test.ts`, `bun test test/runs.test.ts`, `bun test test/cli.test.ts`, `bun test test/api.test.ts`, `bun test test/ui-workbench.test.tsx`, and `bun run typecheck`.
- 09:58 - Updated `docs/cli.md`, `docs/agent-runtime.md`, `docs/agent-authoring.md`, example agents, `ARCHITECTURE.md`, and `MEMORY.md` to match the new contract.
- 10:20 - Started the JSON-output reliability pass after tracing the current run pipeline and confirming that successful runs persist only free-form `finalText`, even when provider transport is already structured.
- 10:42 - Agreed on a forward-only redesign: remove `run.md` and SQLite, persist one canonical `result.json` plus raw `stdout.log`/`stderr.log`, and keep all runs under `~/.aiman/runs/`.
- 11:18 - Replaced markdown run storage with canonical `result.json`, moved global runs to `~/.aiman/runs/<YYYY-MM-DD>/<timestamp-run-id>/`, and switched run discovery to direct filesystem scanning.
- 11:31 - Added a runtime-enforced JSON success contract, updated Codex/Gemini completion parsing, and made invalid structured output fail the run.
- 11:40 - Updated CLI/TUI/API-facing run types and views to the new result-first model, removed the old `run-index.ts` and `run-doc.ts` modules, and verified with `bun run typecheck` plus `bun test`.
- 12:05 - Resolved the leftover CLI regression by migrating `test/cli.test.ts`'s fake Codex last-message output to the structured JSON envelope, then re-ran `bun test test/cli.test.ts` and `bun test`.
- 12:18 - Ran a post-migration cleanup pass, removed two unused test imports, updated stale `run.md`/`prompt.md` wording, and re-verified with `bun run lint`, `bun run typecheck`, `bun test test/cli.test.ts`, and targeted run tests.
- 12:24 - Removed the now-unused `stderrLog`/`stdoutLog` completion-plumbing from the provider parse path, then re-ran lint, typecheck, and the affected CLI/run/provider/API/UI suites.
- 12:41 - Updated the root `aiman-profile-authoring` skill to the current agent contract, added reusable references for agent design and debugging, and created a new `docs/agent-debugging.md` guide.
- 12:49 - Tightened the main authoring docs, CLI/runtime guidance, and example agents so they now emphasize stop conditions, task-specific `result` shapes, and the practical `runs show` / `runs inspect` debugging workflow.
