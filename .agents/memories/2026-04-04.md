# Daily Memory Log - 2026-04-04

## Tasks

- [x] Review the current cleanup branch for incomplete removals, dead code, and regressions.
- [x] Define the native-only provider-context contract for `aiman`, including what belongs in profiles vs `AGENTS.md` vs skills.
- [x] Add layered `aiman` config loading for `~/.aiman/config.json` and optional `<repo>/.aiman/config.json` with deterministic precedence.
- [x] Refactor Codex and Gemini adapters to preserve native context discovery and map configured context filenames into provider-native settings.
- [x] Remove `aiman`-managed skill handling so providers discover skills natively from the repo.
- [x] Keep launch metadata limited to `aiman`-owned context file configuration instead of adding extra review-only evidence such as git SHA capture.
- [x] Update CLI/API/docs/tests to match the native-only context model and remove stale managed-context assumptions.
- [x] Convert the workbench Runs list into a compact table with explicit column headers.
- [x] Make run status visually distinct in the Runs table, including an animated ASCII indicator for active runs.
- [x] Show compact started timestamps and elapsed/completed duration in the Runs table.
- [x] Add focused workbench coverage for the new Runs table presentation and verify the touched TUI files.
- [x] Clean up the CLI commands to match the TUI's workbench model.
- [x] Rename the `sesh` command to `runs` to align with the TUI "Runs" workspace.
- [x] Consolidate run management subcommands (list, show, logs, inspect, stop) under the `runs` command.
- [x] Simplify the `run` command to be purely for launching profiles, removing virtual subcommands.
- [x] Delete legacy `agent` command files (`agent.ts`, `check-agent.ts`, `create.ts`, `list.ts`, `show.ts`).
- [x] Rename run management implementation files to `run-*` for consistency.
- [x] Rename `skills.ts` to `skill-list.ts` for consistency with `profile-list.ts`.
- [x] Update `README.md`, `docs/cli.md`, and `MEMORY.md` to reflect the new command structure.
- [x] Update CLI tests to use the new `runs` command and unified `run` launch path.
- [x] Fix casing in UI workbench tests to match the actual uppercase rendered titles.
- [x] Audit the repo for dead files, oversized modules, and the path to an importable `aiman` package.
- [x] Fix the OpenTUI launch regression and remove the current `bun test` failure in `test/ui-workbench.test.tsx`.
- [x] Remove stale compatibility leftovers (`neo-blessed`, unused text helpers, and redundant agent aliases where safe).
- [x] Expose a strong public `aiman` package API for scripts and refactor CLI/TUI entrypoints to consume it.
- [x] Split oversized run and UI modules into smaller focused files where the new package API needs clear seams.
- [x] Update docs and architecture notes for the package API and final structure, then verify with typecheck/tests/build.
- [x] Centralize all relevant configuration options into project-level and user-level JSON files.
- [x] Refactor `AimanConfig` schema to include defaults for provider, model, mode, reasoning effort, timeouts, and environment variables.
- [x] Make the `createAiman()` package API asynchronous to support configuration loading.
- [x] Update authored and builtin profiles to respect configuration defaults for `model` and `mode`.
- [x] Refactor providers and run execution to draw from the resolved configuration instead of hardcoded constants.
- [x] Verify configuration merging and overriding with new tests in `test/config.test.ts`.
- [x] Rename the public `profile` model to `agent` across the CLI, package API, docs, and project paths.

## Current Status

- Current focus: none.
- Last completed action: Renamed the public `profile` model to `agent`, moved authored project/user paths to `.aiman/agents`, aligned the CLI/package API/docs, and verified with `bun run check`.
- Blocked by: none.

## Progress Log

- 22:56 - Finished the `profile` -> `agent` rename across the public CLI, package API, TUI copy, docs, and project/user agent directories; kept run persistence compatibility where needed, corrected tests that still expected implicit `AGENTS.md` context defaults, and verified the final state with `bun run check`.
- 23:06 - Removed the last implicit config fallback so `contextFileNames` is now opt-in only: when unset, `aiman` leaves bootstrap file selection to the downstream provider's native behavior; updated the config test and relevant docs, then re-verified with `bun run check`.
- 22:42 - Removed the expanded public config surface from `src/lib/config.ts` and `src/lib/types.ts`, keeping layered config focused on shared `contextFileNames` only while moving timeouts/env allowlists back to internal constants.
- 22:50 - Restored the explicit profile contract across `src/lib/profiles.ts`, the package API, CLI/TUI callers, and tests so provider/model/mode/reasoning live in the profile instead of hidden config defaults.
- 22:58 - Fixed stale `aiman run show/logs/inspect` strings in run metadata and rendered operator views, updated stale `sesh`/config docs, and finished with a clean `bun run check`.
- 22:18 - Reviewed the config surface and concluded it is over-expanded: `contextFileNames` is the only clearly durable repo bootstrap setting, while `defaultProvider`, `defaultModel`, `defaultMode`, and `defaultReasoningEffort` add low-value authoring indirection, and `allowedEnvKeys`, `env`, `timeoutMs`, `killGraceMs`, and `mcpDetectionTimeoutMs` are internal runtime knobs that should not all be part of the main public config contract.
- 22:05 - Reviewed the cleanup branch and found that the async/config refactor is incomplete: `bun run typecheck` currently fails in `src/lib/profiles.ts`, profile commands, TUI wiring, and provider-adapter tests; the run-management rename also left stale `aiman run show/logs/inspect` strings in runtime outputs; and `defaultMode` now accepts provider-native values that the adapters still treat as `safe`.
- 20:15 - Expanded `AimanConfig` and `ResolvedAimanConfig` in `src/lib/types.ts` to include `defaultProvider`, `defaultModel`, `defaultMode`, `defaultReasoningEffort`, `timeoutMs`, `killGraceMs`, `mcpDetectionTimeoutMs`, `env`, and `allowedEnvKeys`.
- 20:25 - Updated `src/lib/config.ts` with comprehensive validation and normalization for the new configuration fields, ensuring project settings override user settings with proper merging for records and arrays.
- 20:35 - Refactored `src/lib/runs.ts` to remove hardcoded defaults and use the resolved configuration for timeouts, kill grace, and run modes.
- 20:45 - Updated `src/lib/providers/shared.ts` to support configurable `allowedEnvKeys`, `env` pass-through, and MCP detection timeouts.
- 20:55 - Made the `createAiman()` package API asynchronous in `src/api/client.ts` and updated all CLI/test callers to properly `await` the client initialization.
- 21:05 - Updated `src/lib/profiles.ts` to make `model` and `mode` optional in profile frontmatter, applying configuration defaults during profile loading for both authored and builtin profiles.
- 21:15 - Verified the new configuration system with expanded tests in `test/config.test.ts` and confirmed overall system stability with a full test run.

## Decisions

- 22:36 - The user-facing product model should use `agent` consistently instead of `profile`; keep internal compatibility only where it reduces churn in persisted run records or lower-level runtime seams.
- 23:04 - `contextFileNames` should not have an implicit fallback value; the config is purely opt-in, and when it is absent `aiman` should defer completely to provider-native bootstrap context behavior.
- 22:40 - Keep the public layered config narrowly focused on shared repo bootstrap context through `contextFileNames`; do not expose per-profile defaults, env passthrough, or internal timeout knobs in the main config contract.
- 22:48 - Provider, model, mode, and reasoning effort remain explicit profile-level settings; built-in profiles should also declare them directly instead of relying on ambient defaults.
- 20:12 - Configuration should be centralized in `config.json` files to reduce hardcoded constants and provide a single point of control for both users and projects.
- 20:40 - The `createAiman()` API must be asynchronous to allow for non-blocking configuration loading from the filesystem during initialization.
- 21:00 - Profiles should not be required to specify a `model` or `mode` if project or user defaults are available, promoting consistency across a workspace.
- 18:12 - Added `src/lib/config.ts` plus home/project `config.json` discovery so `aiman` can resolve one shared `contextFileNames` list per repo with project-over-home precedence.
- 18:18 - Refactored run preparation to stop injecting `AGENTS.md#Aiman Runtime Context`, record effective `contextFiles` in launch snapshots, and hand shared native context file names to both adapters.
- 18:24 - Updated the Codex adapter to preserve native `AGENTS.md` behavior while mapping extra configured file names through `project_doc_fallback_filenames`, and updated the Gemini adapter to pass the same shared file names through its child settings overlay.
- 18:32 - Updated profile scaffolds, docs, and tests to the native-only context model and verified with `bun run typecheck`, `bun test`, `bun run build`, and `bun run lint` (lint passes with pre-existing warnings only).
- 17:05 - Chose the product direction for upcoming agent work: `aiman` should use native provider context discovery by default, keep strict run/output/artifact contracts in profiles, use `AGENTS.md` as repo guidance, and move harness behavior such as preferred context filenames into layered config instead of managed prompt injection.
- 15:24 - Reworked the runs left pane into a dedicated single-line table renderer with headers for status, profile, project, started, duration, and run id, while keeping the existing detail-pane drill-down behavior unchanged.
- 15:31 - Added shared run-format helpers for compact timestamps, live/completed duration, and human status labels, then wired a lightweight 250 ms animation frame in `src/tui/aiman-app.tsx` so active runs visibly pulse between ASCII spinner states.
- 15:35 - Added focused workbench coverage for the new runs table presentation and verified the touched TUI path with `bun test test/ui-workbench.test.tsx`, `bun run typecheck`, and `bun run build`, all passing.
- 15:02 - Reviewed the current Bun/OpenTUI runs workspace, confirmed the left pane still uses the generic two-line `WorkbenchList`, and chose to replace only that pane with a compact table while keeping the existing drill-down detail flow.
- 10:45 - Deleted legacy `agent` files and renamed `sesh.ts` to `runs.ts`, and run management files to `run-*`.
- 10:52 - Refactored `src/cmd/run.ts` to remove subcommand switching and updated `src/cmd/runs.ts` to include the `stop` command.
- 11:05 - Updated `docs/cli.md` and `README.md` with the new command surface (`aiman runs ...` and launch-only `aiman run ...`).
- 11:15 - Updated `test/cli.test.ts` to reflect the new command structure and fixed the `yargs` suggestion assertion.
- 11:20 - Updated `test/ui-workbench.test.tsx` to fix casing mismatches in rendered page titles (`START` vs `Start`).
- 11:25 - Verified changes with `npm run typecheck` and `bun test test/cli.test.ts`, both passing.
- 12:10 - Audited the current tree and confirmed dead/stale candidates in `src/cmd/install-skill.ts`, `src/tui/text.ts`, and `src/types/neo-blessed.d.ts`.
- 12:15 - Identified the main split seams for future refactors: `src/lib/runs.ts`, `src/lib/run-store.ts`, `src/tui/aiman-app.tsx`, and `src/tui/workbench-view.tsx`.
- 12:20 - Verified the current repo does not pass `bun test`; the `AimanWorkbench` launch flow regression is failing in `test/ui-workbench.test.tsx`.
- 12:35 - Added a public `createAiman()` facade under `src/api/` and `src/index.ts`, then rewired the CLI command handlers to consume that facade instead of calling internal modules directly.
- 12:45 - Removed dead compatibility leftovers: `src/cmd/install-skill.ts`, `src/lib/agents.ts`, `src/tui/text.ts`, and `src/types/neo-blessed.d.ts`.
- 12:55 - Split the old `src/tui/workbench-view.tsx` into `src/tui/workbench-shell.tsx` and `src/tui/workbench-workspaces.tsx`.
- 13:05 - Replaced launch-critical task entry with a controlled keyboard-first draft buffer in `src/tui/aiman-app.tsx` so the workbench no longer depends on hidden textarea focus state.
- 13:12 - Added `test/api.test.ts` to cover the new importable package surface and threaded `projectRoot` through `src/lib/runs.ts` so package runs do not fall back to the caller's process CWD.
- 13:20 - Updated `README.md`, `docs/cli.md`, `MEMORY.md`, and `ARCHITECTURE.md` for the package API and current workbench structure.
- 13:25 - Verified the final state with `bun run typecheck`, `bun test`, and `bun run build`, all passing.

## Decisions

- 17:05 - Move `aiman` toward a native-only context model: providers should read repo context files such as `AGENTS.md` natively, while profiles continue to own strict output, artifact, and scope contracts; `aiman` should configure preferred context filenames and record launch evidence instead of manually injecting managed runtime context.
- 18:12 - Shared provider bootstrap context belongs at the harness level through layered `config.json` files, with one ordered `contextFileNames` list per repo and no per-profile overrides.
- 18:18 - Launch snapshots should record effective native `contextFiles` rather than an injected `projectContextPath`, since the provider now discovers bootstrap context natively.
- 19:12 - `aiman` should not manage skills or record extra git-based forensic metadata; its durable launch contract is the shared `contextFileNames` configuration plus the existing run record and logs.
- 15:24 - The Runs overview should use a dedicated operator table instead of the generic two-line list; show project names on wider terminals, fall back to a narrower column set on smaller widths, and keep the existing right-side detail inspector as the expanded view.
- 15:31 - Running state should be obvious without pretending to know percent complete: use a small looping ASCII spinner in the status column and let the duration column tick as elapsed time for active runs.
- 15:02 - Keep the Runs-page refresh/drill-down behavior intact and localize this UX pass to the left-pane presentation plus shared run-format helpers instead of reworking the wider workbench layout.
- 10:40 - Unify run management under a plural `runs` command to match the TUI's "Runs" tab, while keeping `run` (singular) for the primary launch action.
- 10:42 - Remove subcommands from `run` entirely to eliminate ambiguity when profile names collide with command names (e.g., a profile named `list`).
- 10:45 - Delete all remaining legacy `agent` files now that the `profile` model is the canonical one.
- 11:20 - Update TUI tests to expect uppercase titles since the `WorkbenchShell` explicitly calls `.toUpperCase()` on the page title for visual style.
- 12:12 - Treat `install-skill.ts`, `ui/text.ts`, and `neo-blessed.d.ts` as removal candidates unless they are intentionally being reintroduced immediately.
- 12:18 - Package extraction should start by defining a small public core API and splitting CLI/TUI adapters around it rather than exporting the current `src/lib` catch-all as-is.
- 12:40 - The package-facing API should bind one resolved project root up front; run launches must not silently fall back to the process CWD when `createAiman({ projectRoot })` is used from another script.
- 12:58 - The workbench `tasks` workspace should own a controlled draft buffer directly in the controller instead of relying on an embedded textarea for launch-critical input.
