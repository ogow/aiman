# Daily Memory Log - 2026-04-02

## Tasks

- [x] Refresh the shared TUI header chrome in `src/ui/components.tsx` to remove extra top spacing and render aligned Claude-like hotkey rows.
- [x] Redesign the main app start screen and letter-based navigation in `src/ui/aiman-app.tsx`.
- [x] Update TUI tests and verify the refreshed Ink surfaces with targeted checks.
- [x] Add the Ink/React repo foundation for TTY UIs: dependencies, TSX config, shared theme layer, and test support.
- [x] Migrate the default no-arg `aiman` app from raw ANSI rendering to an Ink + `@inkjs/ui` surface without changing run/profile logic.
- [x] Migrate `aiman sesh top` onto the shared Ink UI layer while keeping the existing command surface and filter behavior.
- [x] Update focused docs, architecture/memory notes, and verification coverage for the new Ink-based TTY surfaces.
- [x] Add a bundled `.agents/skills/ink-ui-docs` skill with local copied Ink UI docs and validate it in-repo.
- [x] Create `codex/import-feature-new` from the current `master` checkout and overlay the downloaded `/Users/ogow/Downloads/aiman.zip` worktree without importing its `.git` metadata.
- [x] Inspect the imported branch status and confirm the downloaded snapshot landed as local changes for review.
- [x] Normalize permissive file and directory modes from the downloaded archive back to standard repo permissions.
- [x] Polish the TUI UX: update header spacing, introduce a centered ASCII art home screen, and enforce an "agent first, then task" workflow by removing the global `n` shortcut.

## Current Status

- Current focus: none.
- Last completed action: Finished the TUI chrome/start-screen refresh, updated the TTY memory notes, and reran focused verification.
- Blocked by: none.

## Progress Log

- 09:32 - Updated the runtime model in `src/lib/agents.ts`, `src/lib/runs.ts`, `src/lib/run-store.ts`, `src/lib/run-render.ts`, and `src/lib/types.ts` so authored agents can declare `contextFiles`, prompts append only those explicit files, run snapshots store `contextFiles` and plain skill names, and run-time skill resolution/preflight is removed.
- 09:41 - Simplified provider behavior by removing Gemini skill allowlisting from `src/lib/providers/gemini.ts` while keeping Codex/Gemini ambient project-instruction isolation intact. Updated `src/lib/paths.ts` so bare `AGENTS.md` no longer defines the project root.
- 09:55 - Added regression coverage in `test/provider-adapters.test.ts`, `test/runs.test.ts`, `test/paths.test.ts`, and `test/cli.test.ts` for prompt context injection, launch metadata, invalid `contextFiles`, the new AGENTS-free root resolution, and the relaxed skill behavior.
- 10:03 - Verified the change with `node --import tsx --test test/provider-adapters.test.ts test/runs.test.ts test/paths.test.ts`, `node --import tsx --test test/cli.test.ts`, `npm test`, `npm run typecheck`, and `npm run lint`, all passing.
- 15:42 - Added `docs/agent-baseline.md`, updated `AGENTS.md`, `README.md`, `docs/cli.md`, and `docs/agent-runtime.md`, and changed `src/lib/agents.ts` scaffolding so new agents get a commented `contextFiles` example pointing at the neutral baseline doc.
- 15:56 - Added `npm run test:provider-contract` plus `test/provider-contract.ts`, which uses the real Codex and Gemini CLIs against temp repos with sentinel `AGENTS.md`, `GEMINI.md`, and explicit baseline context files.
- 16:08 - The first live contract run exposed two real issues: Codex needed the temp repo initialized as Git, and Gemini headless multiline prompts were unreliable through argv on Windows.
- 16:18 - Fixed the Gemini transport by switching `src/lib/providers/gemini.ts` to `--prompt ""` plus stdin, updated adapter/fixture tests, reran `npm test`, and reran `npm run test:provider-contract` successfully for both real providers.
- 18:18 - Investigated unexpected Codex warnings from repo `.codex/config.toml` agent-role entries, confirmed they were separate from `contextFiles`, and fixed `src/lib/providers/codex.ts` to add `--config 'agents={}'` so authored `aiman` runs ignore repo-defined Codex agent roles while keeping project MCP config available.
- 18:20 - Updated the Codex adapter test plus docs in `README.md`, `docs/cli.md`, and `docs/agent-runtime.md`, then reran `node --import tsx --test test/provider-adapters.test.ts` and `npm run test:provider-contract`, both passing.
- 17:02 - Fixed two follow-up review findings: `src/lib/paths.ts` now ignores `$HOME` itself as a project root even when it is a Git checkout, and `src/lib/providers/shared.ts` now force-kills Windows `.cmd` provider trees when MCP detection times out. Added focused regressions in `test/paths.test.ts` and `test/provider-adapters.test.ts`, then reran `node --import tsx --test test/paths.test.ts test/provider-adapters.test.ts`, `npm run typecheck`, and `npm test`.
- 17:34 - Added `docs/agent-authoring.md`, linked it from `README.md`, `docs/cli.md`, `docs/agent-runtime.md`, and `AGENTS.md`, and updated the bundled `.agents/skills/aiman/` guidance so parent agents ask focused follow-up questions, use a reliability checklist, and treat skill names as declared metadata rather than locally resolved files. Ran Prettier on the touched docs and `python C:\\Users\\oskar.wulff\\.codex\\skills\\.system\\skill-creator\\scripts\\quick_validate.py .agents\\skills\\aiman`.
- 19:03 - Added `src/cmd/check-agent.ts` plus static validation support in `src/lib/agents.ts` so `aiman agent check <agent>` now reports blocking errors separately from structural warnings, resolves malformed files by filename when needed, and exits `1` only when blocking issues exist.
- 19:11 - Expanded CLI coverage in `test/cli.test.ts` for clean, warning-only, blocking-error, and malformed-file `agent check` cases, then reran `node --import tsx --test test/cli.test.ts` and `npm run typecheck`, both passing.
- 19:18 - Tightened the generated agent scaffold around uncertainty handling and explicit output shape, refreshed `README.md`, `docs/cli.md`, `docs/agent-runtime.md`, `docs/agent-authoring.md`, and the bundled `aiman` skill references to teach the `create -> show -> check` flow, and added three reusable example agent files under `docs/examples/`.
- 20:22 - Refactored the public runtime model around profiles, runs, and local `aiman` skills: added `.aiman/profiles` and `.aiman/skills`, built-in `build`/`plan` profiles, `AGENTS.md` runtime-context extraction, local skill selection/suggestion, and profile migration from legacy `.aiman/agents`.
- 20:31 - Replaced the public command surface with `profile`, `skill`, and `run`; made `aiman` with no args open the new single-pane TUI; and kept Codex/Gemini as backend-only adapters with explicit `safe`/`yolo` mode mapping.
- 20:44 - Replaced the outdated CLI/path/provider/run tests with focused vNext coverage, then verified the repo with `npm run typecheck`, `npm test`, and `npm run build`.
- 20:57 - User clarified that the new `ink-ui-docs` skill belongs under `.agents/skills/` in this repo, not in the Codex user-scope skill directory.
- 21:01 - Copied the validated `ink-ui-docs` skill into `.agents/skills/ink-ui-docs`, including bundled `references/upstream-docs/*.md`, then verified it with `quick_validate.py` and `node .agents\\skills\\ink-ui-docs\\scripts\\fetch-doc.mjs text-input`.
- 17:22 - Added the Ink runtime foundation: installed `react`, `ink`, `@inkjs/ui`, `ink-testing-library`, removed `neo-blessed`, and updated the TypeScript/test config for TSX-based TTY UI modules.
- 17:48 - Replaced the default no-arg app with a shared Ink screen in `src/ui/aiman-app.tsx`, plus reusable theme, terminal, and pane helpers under `src/ui/`.
- 18:01 - Replaced `aiman sesh top` with the shared Ink dashboard in `src/ui/top-screen.tsx`, restored the `sesh` command registration in `src/cmd/index.ts`, and kept the existing `--filter` behavior.
- 18:11 - Added `test/ui-components.test.tsx` for shared pane/markdown rendering coverage and reran `npm run typecheck`, `npm test`, and `npm run build`, all passing.
- 18:19 - Updated `MEMORY.md`, `ARCHITECTURE.md`, `README.md`, and `docs/cli.md` so the documented command surface and TTY architecture match the new Ink-based implementation.
- 18:27 - Fixed an Ink runtime update loop in `src/ui/hooks.ts`, `src/ui/aiman-app.tsx`, and `src/ui/top-screen.tsx` by replacing effect-event-based polling/resize dependencies with ref-backed refresh functions and no-op size updates when dimensions are unchanged. Reran `npm run typecheck` and `npm test`, both passing.
- 18:41 - Refined the Ink layout direction toward a quieter run-first dashboard: simplified shared pane/footer chrome, made the default app surface active/recent run summaries more directly, and rebuilt `src/ui/top-screen.tsx` so active and recent runs stay visible together instead of hiding behind a single filtered list. Reran `npm run typecheck`, `npm test`, and `npm run build`, all passing.
- 18:56 - Changed the main app interaction model in `src/ui/aiman-app.tsx` toward direct navigation: added a real start page, a dedicated agents table, a filterable runs table, single-key page shortcuts (`g/a/n/r/i/o/l/p`), and true `Esc` back-stack behavior instead of only resetting focus. Reran `npm run typecheck`, targeted tests, and `npm run build`, all passing.
- 10:10 - Created a fresh local branch `codex/import-feature-new` from the clean current-repo `master` checkout so the downloaded worktree could be imported without disturbing the existing branch.
- 10:11 - Overlaid `/Users/ogow/Downloads/aiman.zip` onto the new branch with `rsync`, excluding the downloaded `.git` metadata and `node_modules`, so the repo now reflects that other-computer snapshot as local branch changes.
- 10:12 - Reviewed `git status` and `git diff --stat`; the import brought over broad CLI/runtime/TUI updates plus new docs, tests, and bundled skills, all left unstaged for follow-up review or commit.
- 10:16 - Normalized the imported archive's filesystem modes across the repo: directories back to `755`, regular files back to `644`, and restored the tracked executable bit on `.husky/pre-commit`.
- 11:30 - Polished the TUI UX by adding a `centerText` utility, adjusting the header spacing in `AppHeader`, and redesigning the `home` view with an ASCII logo. Removed the global `n` shortcut to enforce the logical "choose agent, then write task" workflow. Verified successful compilation.
- 21:44 - Reworked `src/ui/components.tsx` into a compact shared header with amber ASCII branding, aligned hotkey rows, and per-screen legend hints. Updated `src/ui/text.ts` with a new `brand` tone and restyled `src/ui/top-screen.tsx` to reuse the same chrome.
- 21:53 - Rebuilt the `aiman` home view in `src/ui/aiman-app.tsx` around a centered branded hero, replaced numeric global navigation with `g/a/t/r`, and exported focused helpers for the new keymap and home-screen rendering.
- 21:58 - Expanded `test/ui-components.test.tsx` to cover header formatting, letter-based navigation helpers, and the new home-screen copy. `node --import tsx --test test/ui-components.test.tsx`, `npm run typecheck`, and `npm run build` passed. Full `npm test` still fails on pre-existing Windows `cmd.exe` and `/private/var` path assertions outside the touched TUI files.

## Decisions

- 09:25 - `aiman` agent files are the primary instruction contract; ambient repo `AGENTS.md` inheritance stays disabled by default.
- 09:27 - Extra repo-specific guidance should be opt-in per agent through explicit `contextFiles`, not blanket project instruction inheritance.
- 09:29 - `skills:` remains useful as declared metadata for authored agents, but `aiman` should not try to resolve or hard-enforce provider-visible skills at run time.
- 15:40 - `docs/agent-baseline.md` is the neutral explicit baseline for authored `aiman` agents, while repo `AGENTS.md` stays a lightweight router for direct host-agent use.
- 16:10 - Gemini headless runs should keep using a child-local settings overlay for context isolation, but the authored prompt itself should travel on stdin with `--prompt ""` so multiline prompts survive Windows command-wrapper launches.
- 16:19 - The strict prompt-isolation promise is now backed by the live `npm run test:provider-contract` suite rather than only adapter wiring tests.
- 18:17 - Codex-backed `aiman` runs should also blank the `agents` config table with `--config 'agents={}'` so repo `.codex/config.toml` role definitions do not leak warnings or role behavior into authored `aiman` runs.
- 18:59 - Reliable-agent authoring should lean on a static `agent check` command, stronger scaffolds, and example agents before adding more runtime complexity.
- 19:01 - `aiman agent check` should stay static in v1: no provider launch, no MCP probing, and separate `errors` vs `warnings` output for both humans and wrappers.
- 20:05 - `aiman` vNext will move to a small-terminal-first human TUI with single-pane mode switching, treat Codex/Gemini as execution backends only, load repo guidance only from the `## Aiman Runtime Context` section in repo-root `AGENTS.md`, and use local `aiman` skills from `.aiman/skills` or `~/.aiman/skills` rather than provider-native skills.
- 20:40 - The public CLI and persisted run contract now optimize for the vNext model first, while only lightweight alias fields remain internally where needed to read older run records.
- 17:14 - The new TTY direction uses a shared Ink + `@inkjs/ui` layer under `src/ui/` for both `aiman` and `aiman sesh top`, while keeping the UI TTY-only, small-terminal-first, and backed by the existing run/profile domain modules.
- 18:25 - Ink polling and terminal-size effects should avoid dependency loops around event-wrapper functions; the shared TTY layer now uses ref-backed refresh callbacks inside `useEffect` so repeated rerenders do not trigger `Maximum update depth exceeded`.
- 18:34 - The interactive TTY surfaces should read as a focused dashboard, not a crowded control panel: keep chrome minimal, show active and recent runs at the same time when possible, and use the right pane primarily for inspection rather than duplicating navigation affordances.
- 18:49 - In the main app, `Esc` should mean back through the view stack rather than “move focus to nav,” and the primary navigation should be single-key and mnemonic so common flows do not depend on tab-cycling.
- 21:41 - The shared TTY chrome should lean into a Claude Code-like feel: compact header spacing, amber ASCII branding, aligned hotkey rows, and a sparse centered start screen rather than a metrics dashboard.
- 10:09 - Import the downloaded repo by overlaying its worktree onto a fresh branch in the current checkout rather than reusing its bundled `.git` data, so the existing local history and remotes stay authoritative.
- 11:30 - Removed the global "n" new task shortcut from the main menu and agents view so users are guided to select an agent first rather than creating an orphaned task. Start screen is now visually focused on navigation options rather than data.

## Carry Forward

- If agent authoring ergonomics need another pass, consider whether `aiman agent create` should eventually support editing `contextFiles` directly instead of only scaffolding the commented baseline example.
