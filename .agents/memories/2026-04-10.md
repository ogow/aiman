# Daily Memory Log - 2026-04-10

## Tasks

- [x] Re-check the project `site-mapper` agent with the `agent-hardening` workflow, including tiny blocked-path and happy-path smoke tasks, and capture whether the real issue is prompt drift or provider/runtime behavior.
- [x] Fix Gemini schema-mode parsing so prose-heavy Gemini responses can still yield a valid structured result when they contain one clear final JSON object, then re-verify `site-mapper`.
- [x] Decide against adding new verification subcommands for now and ship a dedicated `agent-hardening` skill that drives existing `aiman` authoring and debugging checks.
- [x] Add a shipped `agent-hardening` skill under `.agents/skills/` with a focused workflow plus bundled hardening and debug-loop references.
- [x] Update agent authoring and debugging docs to recommend the new `agent-hardening` skill as the default helper for creating and repairing reliable agents.
- [x] Create the first project `site-mapper` demo agent as a bounded katana-based website reconnaissance specialist.
- [x] Pin the project `web-explorer` agent back to `gemini-2.5-flash-lite` and re-verify that it uses one named headed Playwright session for a smoke run.
- [x] Prevent the project `web-explorer` agent from ending successfully on unfinished planning prose during broad exploration tasks.
- [x] Tighten the project `web-explorer` agent so Gemini starts browser work immediately instead of spending time on unnecessary skill activation or planning.
- [x] Re-verify the updated `web-explorer` agent with a small live smoke run and inspect the resulting run/session timing.
- [x] Switch the `web-explorer` prompt to an explicit one-time `playwright-cli` skill activation path and tighten the command rules around artifact capture.
- [x] Make the project `web-explorer` default to headed Playwright browser launches so the operator can watch the session.
- [x] Force the project `web-explorer` agent to use one named Playwright session for the whole run so all commands target the same visible browser.
- [x] Fix the `parseFrontmatter` utility to correctly throw `UserError` on invalid YAML when frontmatter delimiters are present.
- [x] Decide not to add a unified verification harness yet; prefer a shipped hardening skill and the existing `aiman` smoke-loop workflow for v1.

## Current Status

- Current focus: none.
- Last completed action: Fixed Gemini schema-mode parsing in the runtime, re-ran `site-mapper` on Gemini against `https://www.marsta.nu`, and confirmed the run now succeeds with a persisted structured result.
- Blocked by: none.

## Progress Log

- 15:18 - Fixed a follow-up TypeScript regression in `src/lib/providers/gemini.ts` by guarding `input.profile` inside `parseCompletion` and cleaning the surrounding brace/indentation structure; `bun test test/provider-adapters.test.ts` and `bun run typecheck` both pass again.
- 19:55 - Started a focused pass on the current `web-explorer` slowness after comparing the latest success and stall traces; the success run spent about 179 seconds in Gemini model time while the actual browser commands only took about 2 seconds, and the stalled run spent almost 2 minutes just deciding to call `activate_skill`.
- 20:02 - Updated `.aiman/agents/web-explorer.md` to use `gemini-2.5-flash-lite`, require immediate direct `playwright-cli` use, demote skill activation to fallback-only behavior, and make simple flows stop after the minimum required evidence.
- 20:04 - Re-verified the updated agent with `bun run src/cli.ts agent check web-explorer --scope project`, `bun run src/cli.ts agent show web-explorer --scope project`, and a live smoke run against `https://example.com`; the new run `20260410T083743Z-web-explorer-0d53ff64` finished successfully in about 17 seconds, made its first tool call after about 2.4 seconds, skipped `activate_skill`, and returned the expected artifact/report flow.
- 20:11 - Switched the prompt again so `activate_skill(playwright-cli)` is explicit and one-time when the tool exists, then re-ran the same smoke task; run `20260410T084123Z-web-explorer-230d2343` completed successfully, started with `activate_skill`, then executed direct `playwright-cli` shell commands and returned the expected result. Added one more prompt guard to forbid unsupported `open --filename` style command construction after seeing Gemini briefly attempt it and recover.
- 20:16 - Updated the agent again so interactive web exploration defaults to headed browser launches (`playwright-cli open --headed <url>`), with headless only when explicitly requested or when headed mode is not possible, and re-ran `bun run src/cli.ts agent check web-explorer --scope project` successfully.
- 20:24 - Investigated the long `marsta.nu` run `20260410T091108Z-web-explorer-8fbc4426` and confirmed the agent did navigate with `click`, `type`, and `press`, but the prompt was still relying on the implicit default Playwright session. Updated `.aiman/agents/web-explorer.md` so every command uses one named session `web-explorer-{{runId}}` and therefore targets the same visible headed browser throughout the run, then re-ran `bun run src/cli.ts agent check web-explorer --scope project` successfully.
- 20:31 - Investigated run `20260410T092848Z-web-explorer-bf8a1789` and confirmed the failure mode was prompt/output drift, not missing navigation: Gemini activated the skill, opened the named headed browser, saved a snapshot, then stopped on "Now, I will..." planning prose and `aiman` accepted that unfinished text as a successful text-mode answer. Tightened the prompt so broad tasks are bounded to a few high-value flows and the agent must never finish on intermediate planning narration.
- 20:45 - Discovered and fixed a regression in `src/lib/frontmatter.ts` where `gray-matter` would fail silently on invalid YAML instead of throwing; added a manual check for unstripped delimiters and verified the fix with `bun test`.
- 12:10 - Added a first-cut project `site-mapper` agent at `.aiman/agents/site-mapper.md` as a schema-mode, bounded katana-based reconnaissance specialist intended to map one site into stable page groups and candidate flows for later follow-up work.
- 12:19 - Narrowed the `site-mapper` scope back to the authored agent only after clarifying that harnesses and evals should be added later on demand; removed the temporary `examples/site-mapper-eval.ts` and `examples/site-mapper-eval-suite.sample.json` scaffold files instead of carrying premature tooling forward.
- 12:38 - Re-ran `site-mapper` with a tiny live smoke task against `https://example.com`; `aiman agent check` still passed cleanly and `katana` was installed, but run `20260410T103913Z-site-mapper-040b6ee0` failed because Gemini activated the skill and drifted into planning prose instead of returning valid schema JSON. Decided the next reliability step should be one unified project-level harness with stable contract assertions, replayable crawl fixtures, and optional live smoke checks rather than more core runtime logic.
- 12:46 - Refined the harness direction toward a seamless author experience: `aiman` should make robust agents easy by standardizing a shared verification workflow with per-agent suites and validators, keeping the runtime thin while making replay evals and smoke checks first-class project conventions.
- 13:18 - Replaced the heavier verification-command direction with a lighter shipped skill approach: added `.agents/skills/agent-hardening/` with a focused SKILL, UI metadata, and hardening/debug-loop references, then updated `docs/creating-agents.md`, `docs/agent-authoring.md`, and `docs/agent-debugging.md` to point users at the skill instead of new CLI surface.
- 13:20 - Promoted the stable repo truth into `MEMORY.md`: the repo now ships a dedicated `agent-hardening` skill for creating and repairing authored agents while keeping the runtime thin and reusing the existing `agent check` plus smoke-run workflow.
- 14:54 - Ran the `agent-hardening` loop on `.aiman/agents/site-mapper.md`: `aiman agent check` passed, `katana` existed locally, the missing-URL blocked path succeeded, but Gemini still failed both the ambiguous-URL and `https://example.com` happy path because it wrapped or prefixed the schema JSON with visible prose.
- 14:57 - Tightened `site-mapper` twice to force immediate blocked-path JSON, forbid progress narration, and require exactly one final JSON object. That fixed the blocked-path cases under Gemini, but the happy path still failed because Gemini continued to emit visible assistant narration and fenced JSON after tool use.
- 14:58 - Switched `site-mapper` from Gemini to Codex (`gpt-5.4-mini`, `reasoningEffort: medium`) as a targeted hardening step. Under Codex the missing-URL and ambiguous-URL smoke tasks returned clean blocked schema results, but the live `https://example.com` crawl returned a valid blocked schema result because DNS resolution failed inside the Codex runtime (`curl: (6) Could not resolve host: example.com`), even though local shell crawls had worked earlier outside the provider runtime.
- 15:02 - Reverted `site-mapper` back to Gemini after clarifying that the agent should stay within Gemini's provider contract. Restored `provider: gemini`, removed the Codex-only `reasoningEffort` field, kept the prompt hardening changes, and re-verified with `aiman agent check` that Gemini now resolves cleanly with reasoning shown as the implicit `none`.
- 15:07 - Investigated the latest Gemini failure run `20260410T130623Z-site-mapper-e25eb1ba` and confirmed the crawl itself succeeded against `https://www.marsta.nu`, but Gemini still prefixed the otherwise valid final JSON with explanatory prose. This showed the remaining problem belonged in Gemini-specific completion parsing, not the generic schema validator or the agent contract.
- 15:10 - Updated `src/lib/providers/gemini.ts` so schema-mode Gemini runs extract the last valid top-level JSON object from prose-heavy assistant output while still honoring top-level provider `error` payloads. Added focused coverage in `test/provider-adapters.test.ts`, and the targeted provider adapter test file now passes.
- 15:10 - Re-ran `site-mapper` on Gemini with `bun run src/cli.ts run site-mapper --task "map the site: https://www.marsta.nu"`; new run `20260410T131006Z-site-mapper-2696abbd` completed successfully with `status: success`, `outcome: done`, a persisted `structuredResult`, and the expected `katana-crawl.jsonl` artifact.
