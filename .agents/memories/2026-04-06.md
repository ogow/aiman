# Daily Memory Log - 2026-04-06

## Tasks

- [x] Fix Gemini artifact workspace access so web-explorer can read run artifacts from `~/.aiman/runs/<runid>/artifacts`.
- [x] Fix Gemini yolo-mode mapping so headless runs keep native skill activation.
- [x] Fix Unix run supervision so timed-out provider descendants do not keep runs stuck alive.
- [x] Harden the `web-explorer` user agent against unsupported tool assumptions that stall Gemini runs.
- [x] Rename the global `navigator` agent to `web-explorer` and tighten its authored contract.
- [x] Update the `aiman-agent-authoring` skill to teach the runtime artifact-storage default.
- [x] Enforce automatic per-run artifact path guidance in runtime prompt assembly.
   - [x] Append a standard artifact-storage addendum to rendered prompts.
   - [x] Update provider/runtime tests for the new prompt contract.
   - [x] Update runtime docs and durable memory for the new artifact rule.
- [x] Create a global `navigator` agent for site exploration.
   - [x] Research and design the agent contract.
   - [x] Create the agent file in `~/.aiman/agents/navigator.md`.
   - [x] Refine the agent for active functional flows (not just links).
   - [x] Add technical artifact capturing (HTML, JS, GET params, POST/PUT/DELETE requests).
   - [x] Enforce strict artifact storage in the run-specific `artifacts/` directory.
   - [x] Verify the agent with `aiman agent check navigator`.
   - [ ] Perform a smoke run if a target URL is provided or can be inferred.

## Current Status

- Current focus: none.
- Last completed action: Fixed Gemini artifact workspace handling by adding the per-run `artifacts/` directory to Gemini's workspace with `--include-directories` and creating that directory before provider launch, which removes the local `read_file` workspace error seen in `web-explorer-a1231258`.
- Blocked by: none.

## Progress Log

- 09:00 - Initialized daily memory for 2026-04-06.
- 09:01 - Started research for the `navigator` agent.
- 09:05 - Created the initial `navigator` agent.
- 09:15 - Refined the `navigator` agent for interactive flow exploration and state mapping.
- 09:25 - Further refined the `navigator` agent to capture deep technical evidence (HTML, JS, GET params, and full network request/response details).
- 09:35 - Added strict instructions to store artifacts in the correct `aiman` run directory (`~/.aiman/runs/<runid>/artifacts`).
- 09:40 - Moved the final updated agent to global (user) scope and verified it.
- 09:50 - Started fixing the runtime-level gap: agents currently receive the artifact path only through optional prompt placeholders and environment variables, so authored agents that omit `{{artifactsDir}}` fall back to the working directory.
- 10:00 - Updated shared prompt assembly to append a standard artifact-storage addendum that points every run at its own `artifacts/` directory unless the task explicitly requires project-file edits elsewhere.
- 10:06 - Updated runtime and authoring docs plus durable memory to describe the new default artifact rule.
- 10:08 - Verified the change with `bun test test/provider-adapters.test.ts` and `bun run typecheck`.
- 10:09 - Ran `bun run lint` to confirm the prompt-builder and test changes still satisfy repo lint rules.
- 10:14 - Updated `/Users/ogow/.agents/skills/aiman-agent-authoring/SKILL.md` so the skill now teaches the automatic runtime artifact-storage note and tells authors to use `{{artifactsDir}}` only when the exact path matters.
- 10:20 - Started refining the user-scope `navigator` agent into a tighter `web-explorer` contract and preparing a clean rename in `~/.aiman/agents/`.
- 10:27 - Drafted a tighter `web-explorer` user-scope agent body focused on interactive flow exploration, high-value evidence capture, and concise reporting without redundant generic artifact-path boilerplate.
- 10:28 - Installed `/Users/ogow/.aiman/agents/web-explorer.md`, removed the old `navigator.md`, and verified the new agent with `bun run src/cli.ts agent check web-explorer --scope user` and `bun run src/cli.ts agent show web-explorer --scope user`.
- 10:33 - Investigated run `web-explorer-7965939f`; the run had no stdout log, no `artifacts/` directory, and stderr showed Gemini trying to call `activate_skill`, which is not available in this runtime.
- 10:36 - Hardened `/Users/ogow/.aiman/agents/web-explorer.md` so it now tells Gemini to use only already-available browser tools, avoid skill activation or tool installation attempts, and stop early when browser tools are missing.
- 10:37 - Revalidated the updated agent with `bun run src/cli.ts agent check web-explorer --scope user` and `bun run src/cli.ts agent show web-explorer --scope user`.
- 10:38 - Confirmed a second issue in run `web-explorer-7965939f`: it continued heartbeating past its configured 5-minute timeout, which points to a separate run-supervision bug beyond the agent prompt itself.
- 10:42 - Inspected the live process tree for the stuck run and confirmed the root cause: the Gemini subprocess spawned MCP helper descendants that inherited stdio pipes, so killing only the direct child was not enough to let the supervisor finish.
- 10:48 - Updated `src/lib/runs.ts` so non-Windows provider runs launch in their own process group and timeout/stop handling kills the full group with `SIGTERM` then `SIGKILL`.
- 10:50 - Added a Unix regression test in `test/runs.test.ts` that reproduces a hanging provider descendant keeping inherited stdio open, then verified it with `bun test test/runs.test.ts`, `bun run typecheck`, and `bun run lint`.
- 10:53 - Stopped the stale run `web-explorer-7965939f`; it is now inactive, though its old `run.md` remains non-terminal because the buggy supervisor exited before persisting a final record.
- 11:05 - Re-checked Gemini CLI internals and confirmed the adapter-level `--include-directories` change was the wrong fix: Gemini natively discovers `~/.agents/skills`, `~/.gemini/skills`, and extension-bundled skills, and `activate_skill` itself adds the chosen skill directory to the workspace after activation.
- 11:08 - Verified the Chrome DevTools extension is enabled for `/Users/ogow/*` and that a previous Gemini session under the same `aiman` temp root successfully activated the `chrome-devtools` skill.
- 11:12 - Reverted the temporary Gemini `--include-directories` workaround from `src/lib/providers/gemini.ts`, removed the matching adapter test, and reverified with `bun test test/provider-adapters.test.ts`, `bun run typecheck`, and `bun run lint`.
- 11:18 - Confirmed the older Gemini adapter really did suppress ambient native Gemini context through an env-driven child settings overlay: historical `src/lib/providers/gemini.ts` set `GEMINI_CLI_SYSTEM_SETTINGS_PATH` to a run-local file with `context.fileName: "__AIMAN_UNUSED_CONTEXT__.md"`.
- 11:20 - Confirmed the current adapter still relies on the same `GEMINI_CLI_SYSTEM_SETTINGS_PATH` mechanism, now pointed at configured context file names instead of the impossible sentinel, so that overlay remains the strongest root-cause suspect for lost extension-skill activation.
- 11:34 - Reproduced Gemini's active-tool state locally without calling the model: skills still discover correctly in non-interactive mode, but `activate_skill` is excluded from the active tool set under `--approval-mode auto_edit`.
- 11:36 - Traced the exclusion to Gemini's default policy rules: `activate_skill` is `allow` in `plan`, `ask_user` in `write`, and non-interactive mode downgrades `ask_user` to `deny`, which explains the missing tool in `web-explorer-7965939f`.
- 11:42 - Verified the policy matrix locally: non-interactive `plan` and `yolo` keep `activate_skill`, while non-interactive `auto_edit` excludes it.
- 11:48 - Updated `aiman` so Gemini `mode: yolo` now launches with `--approval-mode yolo` instead of `auto_edit`, and aligned provider capability text, docs, and durable memory with the new mapping.
- 11:50 - Verified the fix with `bun test test/provider-adapters.test.ts`, `bun run typecheck`, and `bun run lint`.
- 11:54 - Relaxed the user-scope `web-explorer` agent again so it prefers Gemini's native browser-skill activation path instead of the temporary "do not activate skills" workaround, then revalidated it with `bun run src/cli.ts agent check web-explorer --scope user` and `bun run src/cli.ts agent show web-explorer --scope user`.
- 18:03 - Investigated run `web-explorer-a1231258` and confirmed the next failure was local workspace access: Gemini could write artifacts under the run directory but then failed `read_file` because `~/.aiman/runs/<runid>/artifacts` was not part of Gemini's allowed workspace.
- 18:04 - Updated the Gemini adapter to pass the per-run artifacts directory through `--include-directories`, and added an adapter test that asserts the workspace argument is present.
- 18:05 - Updated durable memory to record that Gemini-backed runs must expose the per-run artifacts directory as part of Gemini's workspace.
- 18:05 - Ran `bun test test/provider-adapters.test.ts`, `bun run src/cli.ts agent check web-explorer --scope user`, `bun run typecheck`, and `bun run lint` after the adapter change.
- 18:06 - Ran a live `web-explorer` smoke test and found a startup invariant in Gemini: `--include-directories` fails if the target directory does not already exist, so the run aborted before provider startup.
- 18:07 - Updated `src/lib/runs.ts` to create the per-run `artifacts/` directory before provider launch and added a regression test in `test/runs.test.ts` to keep that invariant covered.
- 18:08 - Reverified with `bun test test/provider-adapters.test.ts`, `bun test test/runs.test.ts`, `bun run typecheck`, and `bun run lint`.
- 18:08 - Reran a live `web-explorer` smoke test; the local artifact-workspace failure no longer reproduced, and the run progressed far enough to hit an upstream Gemini 429 capacity error for `gemini-3-flash-preview` instead of any local workspace or artifact-path rejection.
- 18:08 - Stopped the live smoke test run `web-explorer-e8184c97` once it was clear the remaining failure was upstream Gemini capacity rather than an `aiman` harness problem.
