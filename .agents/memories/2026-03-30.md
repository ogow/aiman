# Daily Memory Log - 2026-03-30

## Tasks

- [x] Trace the current `run` and `inspect` flow to locate prompt assembly, persisted files, and any stale legacy state that could confuse runs.
- [x] Narrow the run prompt footer and make report/artifact persistence optional so authored agents stay more deterministic.
- [x] Simplify persisted run files to one canonical `run.json`, keep legacy runs readable, and make empty logs optional.
- [x] Improve the human `run` and `inspect` output so it is easier to understand what happened and how to inspect the exact prompt.
- [x] Update tests, docs, and verification for the run-layout cleanup.
- [x] Replace the new `run.json` canonical record with one canonical `run.md` that combines deterministic frontmatter and the final Markdown result body.
- [x] Remove the separate new-run `report.md` contract by folding report metadata and artifacts into `run.md`.
- [x] Update inspect, parser logic, fixtures, docs, and verification for the single-file `run.md` model using a frontmatter package instead of custom parsing.
- [x] Switch Codex and Gemini adapters to plain-text provider output instead of provider JSON output.
- [x] Restore Codex final-text selection so noisy stdout does not override the persisted last assistant message.
- [x] Add explicit agent scopes and shared agent metadata so project and user agents can coexist cleanly.
- [x] Implement `aiman create` with required scope selection and deterministic structured agent scaffolding.
- [x] Extend `list`, `show`, and `run` to resolve project and user agents together, and persist the resolved source in `run.md`.
- [x] Update tests, fixtures, and docs for dual-scope agents and the new create flow.
- [x] Audit the current CLI/runtime structure for architectural drift, compensating code, and non-idiomatic integrations.
- [x] Tighten `src/lib/run-doc.ts` so artifact entries cannot resolve outside the run `artifacts/` directory.
- [x] Make `aiman list` apply the same project-over-user precedence contract as `show` and `run`.
- [x] Remove or properly align the dead `reasoning-effort` CLI/runtime surface so authored agent options match actual provider behavior.
- [x] Align persisted log-path metadata with `inspect` stream reading so the run-store contract stays internally consistent.
- [x] Update tests, docs, and verification for the cleanup pass.
- [x] Restore `reasoning-effort` as a real Codex feature and align repo policy to avoid backward-compatibility work during active development.
- [x] Remove redundant persisted prompt/log path metadata and other no-op wrappers that do not change current runtime behavior.
- [x] Sync the project docs to the current CLI/runtime behavior and remove stale design-contract language.

## Current Status

- Current focus: none
- Last completed action: Synced the main docs to the current implementation and re-verified the repo.
- Blocked by: none

## Progress Log

- 08:47 - Reviewed `run`, `inspect`, provider adapters, run persistence, and the latest local `.aiman/` runtime files to separate current behavior from stale legacy state.
- 08:50 - Confirmed the current test agent prompt only includes the authored body plus a small runtime footer in `prompt.md`; the much larger "Repository Rules / Memory Workflow" prompt comes from stale legacy `.aiman/state.json` and `.aiman/traces/` files, not the current run path.
- 09:02 - Chose a cleanup direction focused on determinism and operator clarity: keep one canonical `run.json` for new runs, stop creating empty logs up front, keep report/artifact files optional, and make prompt inspection a first-class inspect path.
- 09:14 - Updated the shared prompt footer and provider adapters so report/artifact paths are clearly optional and no extra task context is implied.
- 09:20 - Began switching runtime persistence to final-state `run.json`, added lazy log creation, and started reshaping `inspect` into a human summary with direct prompt access.
- 09:31 - Finished the persistence migration so new runs write one canonical `run.json`, legacy `result.json` remains readable through `inspect`, and empty `stdout`/`stderr` logs are no longer created by default.
- 09:36 - Updated `aiman run` to point users at `inspect`, added `inspect --stream prompt`, and changed the default inspect output into a concise human summary with prompt/log/report locations.
- 09:44 - Added regression tests for prompt inspection, human inspect summaries, and the absence of fresh `result.json` and empty `stderr.log` files; verified `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and targeted `prettier --check`.
- 10:02 - Agreed to simplify further: replace the new canonical `run.json` with a single `run.md` and stop creating separate new-run `report.md` files.
- 10:12 - Removed the legacy compatibility branch after confirming the new-only direction, replaced the custom run document parser with `gray-matter`, and rewired inspect/tests/fixtures around canonical `run.md`.
- 10:23 - Re-ran `npm run lint`, `npm run typecheck`, `npm test`, `npm run build`, and targeted `prettier --check`; all passed after the `gray-matter` and new-only `run.md` cleanup.
- 10:34 - Removed the provider JSON output flags from Codex and Gemini, simplified both adapters to treat `stdout` as plain text, updated the provider/run tests, and re-verified lint, typecheck, tests, build, and targeted Prettier checks.
- 14:43 - Fixed the Codex plain-text parsing regression by preferring `.codex-last-message.txt` over noisy stdout when that file exists, added adapter coverage for both noisy-stdout and no-fallback cases, and re-ran `npm test`.
- 15:08 - Loaded the TypeScript style rules plus the repo memory-maintenance guidance, then traced `paths`, `agents`, `run`, `inspect`, and `run-store` to map where explicit agent scope and source metadata need to land.
- 15:18 - Added shared `AgentScope`/scoped-agent metadata, taught `paths` and `agents` about project plus user agent roots, and implemented project-over-user lookup precedence with explicit scoped lookup when requested.
- 15:29 - Added `aiman create <name>` with required `--scope`, `--provider`, and `--description`, plus deterministic structured agent scaffolding and TTY prompting for missing instructions.
- 15:40 - Extended `list`, `show`, `run`, and persisted `run.md` metadata to surface resolved agent scope/path, then updated CLI/run/provider tests and fixture run documents for the new source metadata.
- 15:46 - Updated `MEMORY.md`, `ARCHITECTURE.md`, `docs/cli.md`, and `docs/agent-runtime.md` for dual-scope agents and the new create flow; verified `npm run typecheck`, `npm run lint`, `npm test`, `npm run build`, and targeted `prettier --check`.
- 15:47 - Confirmed repo-wide `npm run format:check` still fails only because the bundled `.agents/skills/yargs/references/` snapshot contains pre-existing unformatted files plus one invalid upstream `yargs.html`; targeted checks for changed files pass.
- 15:59 - Tightened the `aiman create` UX after spotting provider confusion: `--model` is now the primary input, provider is inferred from recognizable model families when possible, and explicit provider/model mismatches now fail fast.
- 16:04 - Simplified the `aiman create` contract again after clarifying the routing goal: both `--provider` and `--model` are now required so agent authoring stays explicit and adapter routing remains boring.
- 17:47 - Completed a repo-wide audit of the current CLI/runtime shape. The structure is mostly clean and idiomatic for a small TypeScript CLI, but three drift points stood out: artifact resolution trusts paths outside `artifacts/`, `list` does not actually apply the documented project-over-user precedence on name collisions, and `reasoningEffort` is exposed in agent authoring even though runtime only emits a discarded warning and never maps it.
- 17:54 - Tightened `run-doc` artifact handling to ignore escaped artifact paths, made `list` collapse lower-priority cross-scope duplicates, removed the dead `create --reasoning-effort` surface while keeping existing frontmatter readable, and taught `inspect` to honor persisted prompt/log paths with a local run-directory fallback.
- 17:54 - Added regression coverage for escaped artifact paths, list precedence, removed `reasoning-effort` authoring, and custom persisted log paths; re-ran `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build`, all passing.
- 18:01 - Verified from the current OpenAI Codex repo that the CLI maps reasoning effort with `--config model_reasoning_effort="..."`, restored `aiman create --reasoning-effort`, wired the Codex adapter to emit that config override, and made Gemini reject `reasoningEffort` explicitly instead of ignoring it.
- 18:01 - Recorded the new repo rule that this project is forward-only during active development unless the user explicitly asks for backward compatibility, then re-ran `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build`, all passing.
- 18:05 - Removed redundant persisted `promptPath` / `stdoutPath` / `stderrPath` / `artifactsDir` metadata from `run.md`, simplified `inspect` log reading back to the run-directory contract, deleted the no-op `buildRunResult` wrapper, and collapsed validation issues to real errors only because the current runtime no longer uses warning-only branches.
- 18:05 - Updated fixtures/tests for the simpler run-file contract and re-ran `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build`, all passing.
- 18:16 - Updated `ARCHITECTURE.md`, `docs/cli.md`, and `docs/agent-runtime.md` to describe the live CLI/runtime behavior instead of stale design intent: run-directory-derived paths, scope precedence, provider-specific reasoning-effort behavior, current `run.md` fields, and the repo's forward-only cleanup rule.
- 18:16 - Re-ran `npm test`, `npm run lint`, `npm run typecheck`, and `npm run build` after the docs sync; all passed.

## Decisions

- 09:02 - Keep `prompt.md` as the source of truth for what was sent to the downstream agent, and expose it through `aiman inspect <runId> --stream prompt`.
- 09:14 - Make report and artifact persistence opt-in at prompt time so authored agents do not infer extra duties unless their authored instructions actually require persisted handoff files.
- 10:02 - One canonical Markdown run record is simpler than separate runtime and report files; use YAML frontmatter for deterministic fields and the Markdown body for the final result.
- 10:12 - Prefer a package over handwritten YAML parsing for the run document path; `gray-matter` keeps the new-only `run.md` implementation smaller and easier to trust.
- 10:34 - Prefer plain-text provider output over provider-native JSON when `aiman` only needs the final answer text; normalize into `run.md` locally instead of making provider JSON part of the core flow.
- 14:43 - In Codex plain-text mode, treat the persisted last-message file as the authoritative final answer and only fall back to raw stdout when that file is missing or empty.
- 15:18 - Agent scope should be explicit at creation time but implicit at lookup time: always consider both project and user agents during `list`/`show`/`run`, and prefer project scope when names collide unless the caller passes `--scope`.
- 15:29 - `aiman create` should scaffold a structured Markdown prompt body instead of writing raw instructions so the main agent gets a reliable, well-formed starting point.
- 15:59 - `aiman create` should treat model choice as the primary authoring input and only ask for provider when it cannot infer the adapter safely from the model family.
- 16:04 - For `aiman create`, requiring both provider and model is clearer than inference because it keeps routing rules explicit and avoids surprising adapter selection.
- 17:47 - Treat the current codebase as structurally healthy overall, but tighten a few contract mismatches and trust boundaries before adding more features so the runtime does not drift into compensating behavior.
- 17:54 - Keep existing `reasoningEffort` frontmatter backward-compatible for already-authored agents, but do not advertise it in `aiman create` until a provider adapter maps it to real runtime behavior.
- 17:54 - Treat persisted prompt/log paths as advisory rather than absolute truth during inspection: use the explicit stored path first, then fall back to the run-local default so copied fixtures and moved run directories remain inspectable.
- 18:01 - This repo should prefer forward-only cleanup over compatibility shims while the CLI and runtime are still changing quickly.
- 18:01 - `reasoningEffort` should stay in the authored agent model because Codex supports it through `--config model_reasoning_effort="..."`; providers that do not support it should fail clearly.
- 18:05 - Run-file path metadata should come from the run directory, not duplicated frontmatter fields, unless the runtime grows a real alternate-path feature later.
- 18:05 - Keep fallbacks only when they are part of the current product contract; remove compatibility-only branches and dead abstraction layers even if tests/fixtures need to change.
- 18:16 - Treat `docs/agent-runtime.md` and `ARCHITECTURE.md` as current-state implementation docs, not aspirational design notes; prefer documenting the code that exists over retaining stale future-looking sections.

## Carry Forward

- Decide whether stale legacy `.aiman/state.json` and `.aiman/traces/` should be cleaned automatically later or just left as unused historical files.
