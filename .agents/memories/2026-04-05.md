# Daily Memory Log - 2026-04-05

## Tasks

- [x] Switch the Gemini adapter to `--output-format json` and parse the structured stdout result instead of trimming plain text.
- [x] Update Gemini-focused tests and runtime docs for the structured stdout contract.
- [x] Switch the Codex adapter to JSONL stdout while keeping `--output-last-message` as the authoritative final-answer path.
- [x] Update Codex-facing tests and runtime docs for the JSONL stdout contract.
- [x] Research current Codex `exec` sandbox and approval behavior for `aiman` automation, especially MCP/browser tooling and artifact writes.
- [x] Harden the Codex adapter so non-interactive runs do not inherit interactive approvals and can write to the external run artifacts directory.
- [x] Add an installable root `skills/aiman-agent-authoring/` bundle that helps create strong generic `aiman` agents.
- [x] Allow Gemini agents to omit `model` so the `gemini` CLI can choose its automatic default model.
- [x] Switch Gemini automatic model selection to an explicit `model: auto` contract and reject that sentinel for non-Gemini providers.

## Current Status

- Current focus: none.
- Last completed action: Replaced the implicit Gemini missing-model behavior with explicit `model: auto`, restricted that sentinel to Gemini, updated docs and the installable authoring skill, and re-verified with focused tests, typecheck, and Prettier.
- Blocked by: none.

## Progress Log

- 14:49 - Reworked the Gemini model contract so agents must declare `model` explicitly again, with `model: auto` meaning automatic Gemini selection; rejected `auto` for non-Gemini providers and updated the Gemini adapter to omit `--model` only for that sentinel.
- 14:52 - Updated CLI and adapter coverage to require explicit `--model auto` for Gemini automatic selection and to reject `--model auto` for Codex.
- 14:54 - Refreshed the CLI/runtime/authoring docs and `skills/aiman-agent-authoring` guidance to teach `model: auto` instead of omitting the model field.
- 14:55 - Verified the revised contract with `bun test test/provider-adapters.test.ts test/cli.test.ts`, `bun run typecheck`, and `./node_modules/.bin/prettier --check`.
- 14:28 - Updated the agent contract so Codex still requires `model` while Gemini can omit it to use the CLI's automatic model selection; patched agent creation, frontmatter rendering, display labels, and detached-run restore to treat missing Gemini models as valid.
- 14:33 - Added coverage for `aiman agent create` without `--model` on Gemini and for the Gemini adapter omitting `--model` from the launch args.
- 14:35 - Updated `docs/cli.md`, `docs/agent-authoring.md`, `docs/agent-runtime.md`, and the installable `skills/aiman-agent-authoring/` guidance to document optional Gemini model selection.
- 14:39 - Verified the change with `bun test test/provider-adapters.test.ts test/cli.test.ts`, `bun run typecheck`, and `./node_modules/.bin/prettier --check`.
- 14:11 - Investigated whether Gemini agents can omit `model` and rely on the CLI's automatic model selection. Confirmed that `src/lib/providers/gemini.ts` already skips `--model` when the agent model is empty, but agent validation, `aiman agent create`, API types, docs, and detached-run resume logic still require a model, so the behavior is not actually supported end to end.
- 14:03 - Added `skills/aiman-agent-authoring/` with a focused `SKILL.md`, generic references for agent selection, contract definition, and prompt scaffolding, plus lightweight `agents/openai.yaml` metadata for install surfaces.
- 14:04 - Verified the new skill files and the updated daily memory with `./node_modules/.bin/prettier --check`.
- 13:58 - Reviewed `MEMORY.md`, the latest daily memory, `docs/agent-authoring.md`, `docs/agent-baseline.md`, and the skill-creator guidance; decided to add a portable root skill bundle with a focused `SKILL.md`, generic references, and lightweight `agents/openai.yaml` metadata.
- 23:15 - Verified current Codex `exec` behavior against the official Codex CLI docs and local `codex exec --help`; confirmed that non-interactive runs should pin approval policy explicitly and that extra writable roots should be granted through `--add-dir` instead of assuming external artifact paths are writable.
- 23:21 - Updated `src/lib/providers/codex.ts` to add `approval_policy="never"` and `--add-dir <artifactsDir>` to Codex runs, expanded the adapter test in `test/provider-adapters.test.ts`, updated runtime docs, and verified the final state with `bun test test/provider-adapters.test.ts`, `bun run typecheck`, and `bun run check`.
- 11:57 - Confirmed from the official Codex docs and local `codex exec --help` that `--json` produces JSONL events on stdout while `--output-last-message` remains the right final-answer file contract.
- 12:04 - Updated the Codex adapter to pass `--json`, refreshed the fake Codex fixtures in `test/runs.test.ts`, `test/cli.test.ts`, and `test/api.test.ts`, and documented that Codex stdout logs are now raw JSONL events.
- 12:09 - Verified the change with `bun run typecheck`, `bun run check`, and a direct Bun smoke script that produced a JSONL `stdout.log` while keeping `run.md` clean and final-text-only.
- 13:08 - Checked the installed Gemini CLI (`gemini 0.36.0`) and confirmed that headless mode supports `--output-format json` and `--output-format stream-json`; the bundled `JsonFormatter` writes one JSON object containing `session_id`, `response`, optional `stats`, and optional `error`.
- 13:14 - Updated `src/lib/providers/gemini.ts` to request `--output-format json`, parse Gemini's structured `response` and `error.message` fields, and reject malformed or missing JSON output instead of trimming raw stdout.
- 13:17 - Added focused provider-adapter coverage for Gemini JSON success and error payloads, updated the runtime/docs wording, and verified with `node --import tsx --test test/provider-adapters.test.ts`, `bun run typecheck`, and `bun run check`.

## Decisions

- 14:49 - Gemini automatic model selection should be explicit in agent frontmatter as `model: auto`, not inferred from a missing field; this keeps the contract inspectable and leaves room for other provider-specific model config later.
- 14:28 - The supported contract is now provider-specific for `model`: Codex agents must declare one, while Gemini agents may omit it to let the downstream `gemini` CLI choose automatically.
- 14:11 - Gemini automatic model selection is not implemented as a supported `aiman` agent contract today; enabling it would require relaxing model requirements across agent creation, validation, docs, and detached-run restore paths, not just the Gemini adapter.
- 14:03 - The installable skill should ship as a self-contained root `skills/aiman-agent-authoring/` bundle with generic `aiman` guidance and repo-discovery instructions, rather than reusing this repo's project-specific docs directly.
- 13:58 - The new agent-authoring skill should stay generic to `aiman`, not this repo, but it should explicitly tell the model to inspect the host repo's norms and runtime docs before drafting or revising an agent.
- 13:08 - For Gemini, the simpler adapter contract is one final JSON object via `--output-format json`, not plain-text stdout and not `stream-json`, because `aiman` only needs a reliable final result parser rather than incremental Gemini event streaming.

## Decisions

- 23:15 - `aiman` should not let `codex exec` inherit approval behavior from ambient Codex config; non-interactive Codex runs should pin `approval_policy="never"` explicitly.
- 23:16 - Because `aiman` stores run artifacts outside the project workspace under `~/.aiman/runs/.../artifacts`, Codex runs should grant that directory explicitly with `--add-dir` so artifact-producing agents remain reliable without widening repo write access.
- 11:57 - For `aiman`, the safest Codex contract is structured JSONL on stdout plus `--output-last-message` for the final answer, so machine parsing improves without reintroducing noisy-stdout final-text parsing.
