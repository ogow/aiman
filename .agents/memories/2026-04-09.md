# Daily Memory Log - 2026-04-09

## Tasks

- [x] Harden the project `web-explorer` agent so browser-tool detection, Playwright artifact capture, and stop behavior are explicit and less fragile under Gemini.
- [x] Route Playwright-based provider writes to the current run `artifacts/` directory so browser agents can save evidence there without turning off Playwright file guards.
- [x] Add a TUI activity formatter that turns provider stdout/stderr into a more readable LLM-focused timeline for run detail views.
- [x] Update the runs workspace detail tabs and launch flow so active runs open on activity first while raw logs remain available.
- [x] Add focused tests for the new activity view and re-verify the touched TUI/runtime helpers.
- [x] Add optional authored-agent `timeoutMs` support so agent files can choose a longer timeout or disable it explicitly while preserving a safe runtime default.
- [x] Expand the standalone `skills/aiman` metadata and workflow so it triggers for agent validation and eval-harness requests, not only agent creation.
- [x] Add focused `skills/aiman` reference guidance for validating authored agents and skills, including when to build a fixed eval harness.
- [x] Tighten `agent create` scaffolds so new authored agents start with stronger XML task boundaries and explicit missing-evidence guidance.
- [x] Strengthen `agent check` warnings so weak freshly-created agents are easier to catch before runtime.
- [x] Update authoring docs/tests for the stronger creation contract and re-verify the touched CLI path.
- [x] Tighten `skills/aiman/SKILL.md` so it teaches the current authored-agent contract and a repeatable workflow for creating reliable `aiman` agents.
- [x] Update `skills/aiman/references/` so the checklist, selection guide, and prompt template reinforce robust agent boundaries, stop conditions, and `text` vs `schema` output design.
- [x] Re-read the touched skill files for contract drift and record whether the revised skill now gives a clearer path to creating strong, well-structured agents.
- [x] Review the agent-authoring documentation set for readability and identify what makes it hard to create a good agent from the current docs.
- [x] Rewrite the core authoring guidance so it teaches a clearer step-by-step workflow for creating strong `aiman` agents, with simpler explanations and better examples.
- [x] Re-verify the updated docs for consistency with the current `run.json` + `resultMode` contract and record the documentation pass in memory.
- [x] Remove legacy `mode` from bundled repo-owned agents and make the shipped examples conform to the current authored-agent contract.
- [x] Update the legacy `aiman-profile-authoring` skill and its references from the old `result.json`/`handoff` contract to the current `run.json` + `resultMode` model.
- [x] Resolve the foreground `aiman run` output contract drift across code and docs, then re-verify the touched surfaces.
- [x] Add a v1 `resultMode` contract to `src/lib/agents.ts` and `src/lib/types.ts`, with `text` as the default and `schema` as the explicit structured mode.
- [x] Rename the canonical run ledger from `result.json` to `run.json` in `src/lib/run-store.ts`, `src/lib/runs.ts`, provider adapters, and operator-facing run types.
- [x] Make the runtime ledger thin and runtime-owned by persisting `finalText` / `structuredResult` instead of one universal model-authored result envelope.
- [x] Update CLI/TUI/API tests and docs to the new run-ledger and result-mode model, then verify with lint, typecheck, and targeted suites.
- [x] Clean up v1 runtime naming by renaming old result-contract helpers/files to the new run-ledger vocabulary and removing dead shared-provider code.
- [x] Remove legacy authored-agent `mode` acceptance from `src/lib/agents.ts` and related runtime types so the public contract matches the current write-enabled provider model.
- [x] Update CLI/docs/tests to reflect the stricter contract and surface the current result contract more clearly in operator-facing agent views.
- [x] Add a short Claude Managed Agents comparison note that records which concepts `aiman` should keep, borrow next, and avoid.
- [x] Expand the `claude-managed-agents` skill to cover prompting, prompting tools, evals, and guardrail harness topics with direct `aiman` mapping.
- [x] Add optional informational `capabilities` declarations to authored agents, surface them through `agent show` / `agent check`, and persist them in the launch snapshot.
- [x] Tighten authoring defaults by adding `Stop Conditions` to scaffolds and built-ins, and warn when authored agents omit that section.
- [x] Align example agents and authoring docs with explicit mixed `resultMode` usage, including one dedicated text-mode example.
- [x] Add one standalone eval harness example plus docs showing how fixed eval suites should use the `createAiman()` API outside the core runtime.
- [x] Add a live config-smoke harness that verifies authored agent settings and shared context config are actually applied during a real run.
- [x] Document the non-unit config-verification workflow so operators know what the harness proves and how to inspect failures.
- [x] Run the new config-smoke verification path locally and record the result.
- [x] Investigate the Codex fallback-context failure and align the smoke harness with the real provider discovery contract.

## Current Status

- Current focus: none.
- Last completed action: Updated the provider adapters to export `PLAYWRIGHT_MCP_OUTPUT_DIR={{artifactsDir}}` for Codex and Gemini runs, allowlisted the related Playwright env overrides, documented the new runtime rule, and verified with `bun test test/provider-adapters.test.ts`.
- Blocked by: none.

## Progress Log

- 19:36 - Investigated live run `20260409T173003Z-web-explorer-7936e0ef` and confirmed the browser did open on `https://example.com/`: Gemini activated the `playwright-cli` skill, ran `playwright-cli open https://example.com/`, and saw `Example Domain`, but then hit Playwright allowed-root errors when the agent tried to write snapshot/screenshot files directly into the external run `artifacts/` directory.
- 19:38 - Updated `.aiman/agents/web-explorer.md` to make tool bootstrap explicit, fix the command vocabulary around real `playwright-cli` commands, require workspace-first capture plus copy/move into `{{artifactsDir}}`, and force immediate final reporting after best-effort browser cleanup.
- 19:39 - Re-ran `bun run src/cli.ts agent check web-explorer --scope project` successfully, then launched a second live smoke run `20260409T173346Z-web-explorer-5661d1d7`; that run stalled before any tool calls despite the provider process staying alive, so the prompt is improved but Gemini still shows an upstream/non-deterministic completion issue beyond the authored agent body.
- 19:47 - Fixed the provider-side artifact root mismatch by allowlisting `PLAYWRIGHT_MCP_OUTPUT_DIR` / `PLAYWRIGHT_MCP_ALLOW_UNRESTRICTED_FILE_ACCESS`, exporting `PLAYWRIGHT_MCP_OUTPUT_DIR={{artifactsDir}}` from both provider adapters, updating `docs/agent-runtime.md` and `MEMORY.md`, and verifying the adapter contract with `bun test test/provider-adapters.test.ts`.

- 18:34 - Finished the TUI runs-detail pass: active launches now open on an Activity tab, raw stdout/stderr moved to a separate Raw tab, Codex JSON event output is rendered as a readable timeline focused on assistant/tool activity, and the summary now lists artifact filenames when a run created outputs.
- 18:37 - Verified the new TUI behavior with `bun run typecheck` and `bun test test/ui-workbench.test.tsx test/run-activity.test.ts`.
- 18:18 - Started a focused TUI pass after deciding the current runs pane exposes mostly raw stdout/stderr; the next useful step is to preserve stream identity during foreground runs and add an activity-first view that emphasizes what the model is doing.
- 18:05 - Added optional authored `timeoutMs` support across `src/lib/agents.ts`, `src/lib/runs.ts`, API types, and CLI surfaces; the runtime now uses `run override > agent timeoutMs > 5 minute default`, and `timeoutMs: 0` disables the timeout instead of timing out immediately.
- 18:09 - Added focused CLI and run tests covering authored timeout parsing, invalid timeout rejection, override precedence, and the `timeoutMs: 0` no-timeout path, then updated `docs/agent-runtime.md`, `docs/cli.md`, `docs/creating-agents.md`, `docs/agent-authoring.md`, and `MEMORY.md` to document the new contract.
- 17:05 - Started a focused pass on the standalone `skills/aiman` after deciding it should trigger for "validate this agent" and "build an eval harness" requests, not only agent creation and refinement.
- 17:09 - Updated `/Users/ogow/.agents/skills/aiman/SKILL.md` so the skill now advertises validation/eval-harness work, points robustness tasks to a dedicated validation reference, and teaches a layered loop of static check, smoke task, inspection, then fixed eval suite.
- 17:11 - Added `/Users/ogow/.agents/skills/aiman/references/eval-validation.md`, extended the contract checklist with blocked-path and skill-trigger questions, and updated `agents/openai.yaml` so the UI metadata reflects the broader create/refine/validate scope.
- 17:12 - Re-read the touched skill files for drift and attempted the bundled `skill-creator` `quick_validate.py` script; it failed immediately because the local Python environment does not have `PyYAML` installed, so verification for this pass is limited to manual file review.
- 14:45 - Updated `src/lib/agents.ts` so built-ins and newly created agents now wrap `{{task}}` in `<task>` tags, normalize ad-hoc one-line `--instructions` into explicit bullets, add stronger missing-evidence and non-invention guidance, and tailor `Expected Output` defaults for `text` versus `schema` agents.
- 14:47 - Strengthened `agent check` to warn when authored agents leave `{{task}}` unwrapped or fail to state what to do when required evidence/context is missing, then added focused CLI tests covering both warnings plus the improved scaffold path.
- 14:49 - Updated `docs/creating-agents.md`, `docs/agent-authoring.md`, `docs/agent-debugging.md`, and `docs/cli.md` to explain the stronger scaffold contract and the new static warnings, then verified the pass with `bun run typecheck`, `bun test test/cli.test.ts`, and `bunx prettier --check`.
- 14:35 - Started a focused authoring-contract pass after deciding the next useful step is not evals yet, but making `agent create` produce better default scaffolds and teaching `agent check` to catch weaker agent bodies earlier.
- 15:28 - Removed repo-specific `docs/*` assumptions from `skills/aiman/SKILL.md` after realizing the skill must be portable when installed into a user's home or another project; the skill now treats local repo docs as optional context, not required inputs.
- 15:16 - Rewrote `skills/aiman/SKILL.md` to make agent creation more repeatable: the skill now forces clearer boundary decisions, explicit result-mode choice, stop conditions, missing-evidence behavior, and a stronger validation loop.
- 15:19 - Updated `skills/aiman/references/contract-checklist.md`, `agent-selection.md`, and `prompt-template.md` so they no longer teach the old `handoff` shape and instead reinforce current `text` vs `schema` authoring patterns with copyable starter templates.
- 15:22 - Ran a legacy-contract grep plus `bunx prettier --write` and `bunx prettier --check` on the touched skill files; result: the standalone skill now gives a materially clearer path to creating strong, well-structured agents.
- 15:02 - Started a focused pass on `skills/aiman/` after noticing the standalone skill still leaves key reliability choices too implicit and its local references still teach outdated structured-output shapes.
- 15:52 - Added `examples/config-smoke.ts` plus `bun run test:config-smoke` as a live non-unit harness that creates a temp repo, runs a real authored agent through `createAiman()`, and verifies launch metadata plus native bootstrap-context visibility.
- 15:54 - Documented the new config-smoke workflow in `docs/agent-evals.md` and re-verified the touched TypeScript with `bun run typecheck`.
- 15:56 - Ran `bun run test:config-smoke`: Gemini passed end to end, while Codex failed the extra-context assertion even though launch metadata included `project_doc_fallback_filenames=["CONFIG_SMOKE.md"]`, exposing a real provider/runtime contract gap to investigate later.
- 16:14 - Investigated the Codex failure against current official Codex docs and found the harness assumption was wrong: Codex checks `AGENTS.md` before fallback names in each directory and includes at most one instruction file per directory, so same-directory fallback files are not stacked with `AGENTS.md`.
- 16:14 - Updated `examples/config-smoke.ts` to use a nested working-directory layout that is valid for both providers, clarified the Codex one-file-per-directory rule in `docs/agent-evals.md`, `docs/agent-runtime.md`, and `MEMORY.md`, then re-ran `bun run typecheck` and `bun run test:config-smoke` successfully.
- 13:18 - Rewrote `docs/agent-authoring.md` so it now leads with the practical rules, explains when an agent should exist, makes `text` vs `schema` easier to choose, adds copyable starter patterns, and shifts the guide from reference-style prose to a concrete workflow for creating good agents.
- 13:21 - Ran `bunx prettier --write docs/agent-authoring.md` and `bunx prettier --check docs/agent-authoring.md` to verify the rewritten guide is formatted cleanly.
- 13:02 - Started a documentation-quality pass focused on whether the current docs are easy to understand and actually teach how to create good agents, with the immediate plan to tighten `docs/agent-authoring.md` and any supporting examples or debug guidance that still read like reference material instead of a practical workflow.
- 12:51 - Removed legacy `mode` from `.aiman/agents/hello.md`, added explicit `resultMode: text` plus `Stop Conditions`, and verified the bundled agent with `bun run dev agent check hello --scope project`.
- 12:54 - Rewrote the repo `aiman-profile-authoring` skill and its `agent-design` / `debugging` references from the old `result.json` / `resultType` / `handoff` language to the current `run.json` + `resultMode` + `finalText|structuredResult` model.
- 12:56 - Resolved the foreground `aiman run` contract drift by keeping the live text-mode `finalText` behavior and updating durable memory/runtime/CLI docs to describe that behavior consistently.
- 12:42 - Reviewed the finished contract refactor as a production-readiness check and found three alignment gaps: a bundled repo agent still uses legacy `mode`, the legacy `aiman-profile-authoring` skill still teaches `result.json` / `handoff`, and foreground `aiman run` behavior/docs disagree about whether success should print `summary` or `finalText`.
- 09:08 - Started the v1 runtime-contract shift after aligning on the article-driven direction: provider-native skills, file-first outputs, and a thin runtime ledger.
- 09:14 - Decided to implement this in one forward-only pass by adding `resultMode`, defaulting agents to `text`, keeping an explicit `schema` mode for structured runs, and renaming the canonical persisted run record to `run.json`.
- 09:42 - Finished the runtime/result-mode migration in the TypeScript code: agents now default to `text`, schema-mode prompts append the JSON contract, and persisted runs now store `finalText` or `structuredResult` in `run.json`.
- 09:55 - Updated the fixture layer and focused test suites so the default path exercises text-mode agents while schema-mode coverage stays explicit where structured results are required.
- 10:07 - Promoted the new contract into `MEMORY.md`, `ARCHITECTURE.md`, and the runtime/authoring/debugging CLI docs so the repo now documents `run.json` as a thin ledger and file-first outputs as the preferred pattern.
- 10:26 - Renamed the shared provider helper module to `src/lib/providers/runtime.ts`, renamed old contract-heavy helpers such as `buildPrompt` and `finalizeCompletedRun`, renamed `src/lib/run-store.ts` to `src/lib/run-records.ts`, and changed terminal run output from `runPath` to `runFile` for clearer naming.
- 10:33 - Removed the unused shared `runCommandCapture` helper, made schema-only parsing helpers internal, and re-verified the runtime with typecheck, lint, and the focused provider/runs/API/CLI/TUI test suites.
- 10:36 - Reviewed the authored-agent setup against the Claude Managed Agents references. Conclusion: the repo is directionally strong on narrow agent definitions, shared bootstrap context, and explicit result contracts, but it still has contract drift around legacy `mode` acceptance and no first-class per-agent capability contract for tools, skills, or resources.
- 10:49 - Narrowed the follow-up to one simplification pass: reject legacy authored-agent `mode` outright, stop carrying legacy `mode` through user-facing run types, and update operator-facing docs/tests before deciding whether a lightweight capability declaration is worth adding later.
- 10:58 - Finished the contract cleanup: `mode` is now rejected explicitly in authored agents, user-facing run results no longer carry stale mode metadata, and `agent show` / `agent check` now surface `resultMode` directly.
- 11:02 - Added `docs/claude-managed-agents-concepts.md` to capture the practical comparison: keep agent/runtime separation, consider descriptive capability visibility next, and avoid recreating hosted environment/session abstractions inside the CLI.
- 11:03 - Re-ran `bun test test/cli.test.ts test/provider-adapters.test.ts`, `bun run typecheck`, and `bun run lint` successfully after the cleanup and comparison-note pass.
- 11:18 - Expanded `.agents/skills/claude-managed-agents/SKILL.md` so the skill now explicitly covers Managed Agents architecture plus prompt engineering, prompting tools, testing/evals, and guardrail harness patterns.
- 11:22 - Added focused reference modules for prompting overview, prompting best practices, prompting tools, testing/evals, hallucination reduction, consistency, jailbreak mitigation, and prompt-leak reduction, each ending with direct `aiman` guidance about whether the change belongs in authored prompts, shared context, harnesses, or the thin runtime.
- 11:24 - Converted `references/prompt-engineering.md` into a short bridge module to reduce overlap, then validated that the router references 19 real modules and that every new module exposes explicit `aiman` mapping.
- 11:42 - Confirmed the current repo already supports API-based harness scripts via `createAiman()`, that the current example agents describe schema-style outputs without declaring `resultMode`, and that `agent show` currently exposes provider rights but no authored capability summary.
- 12:06 - Added optional informational `capabilities` to the authored-agent contract and API/CLI surfaces, persisted capabilities into the run launch snapshot, and surfaced them in `agent show`, `agent check`, and run inspection without changing runtime behavior.
- 12:11 - Tightened authored prompt defaults by adding `Stop Conditions` to the built-in/scaffolded prompt shape and making `agent check` warn when that section is missing.
- 12:17 - Updated the example agents and authoring/runtime/CLI docs so schema-shaped examples now declare `resultMode: schema`, added one dedicated text-mode example, and documented the new `capabilities` and eval-harness patterns.
- 12:21 - Added `examples/eval-harness.ts` plus `examples/eval-suite.sample.json` and `docs/agent-evals.md` as the first fixed-suite harness example for `createAiman()`-based agent evaluation outside the core runtime.
- 12:26 - Re-ran `bun run typecheck`, `bun test test/cli.test.ts test/api.test.ts test/provider-adapters.test.ts test/runs.test.ts`, `bun run lint`, and a usage-level invocation of `bun run examples/eval-harness.ts`.
