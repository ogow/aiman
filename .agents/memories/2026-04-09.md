# Daily Memory Log - 2026-04-09

## Tasks

- [x] Review the agent-authoring documentation set for readability and identify what makes it hard to create a good agent from the current docs.
- [x] Rewrite the core authoring guidance so it teaches a clearer step-by-step workflow for creating strong `aiman` agents, with simpler explanations and better examples.
- [x] Re-verify the updated docs for consistency with the current `run.json` + `resultMode` contract and record the documentation pass in memory.
- [x] Remove legacy `mode` from bundled repo-owned agents and make the shipped examples conform to the current authored-agent contract.
- [x] Update the legacy `aiman-profile-authoring` skill and its references from the old `result.json`/`handoff` contract to the current `run.json` + `resultMode` model.
- [x] Resolve the foreground `aiman run` output contract drift across code and docs, then re-verify the touched surfaces.
- [x] Add a v1 `resultMode` contract to `src/lib/agents.ts` and `src/lib/types.ts`, with `text` as the default and `schema` as the explicit structured mode.
- [x] Rename the canonical run ledger from `result.json` to `run.json` in `src/lib/run-store.ts`, `src/lib/runs.ts`, provider adapters, and operator-facing run types.
- [x] Make the runtime ledger thin and runtime-owned by persisting `finalText` / `structuredResult` instead of one universal model-authored result envelope.
- [x] Update CLI/TUI/API tests and docs to the new run-ledger and result-mode model, then verify with lint, typecheck, and targeted suites.
- [x] Clean up v1 runtime naming by renaming old result-contract helpers/files to the new run-ledger vocabulary and removing dead shared-provider code.
- [x] Remove legacy authored-agent `mode` acceptance from `src/lib/agents.ts` and related runtime types so the public contract matches the current write-enabled provider model.
- [x] Update CLI/docs/tests to reflect the stricter contract and surface the current result contract more clearly in operator-facing agent views.
- [x] Add a short Claude Managed Agents comparison note that records which concepts `aiman` should keep, borrow next, and avoid.
- [x] Expand the `claude-managed-agents` skill to cover prompting, prompting tools, evals, and guardrail harness topics with direct `aiman` mapping.
- [x] Add optional informational `capabilities` declarations to authored agents, surface them through `agent show` / `agent check`, and persist them in the launch snapshot.
- [x] Tighten authoring defaults by adding `Stop Conditions` to scaffolds and built-ins, and warn when authored agents omit that section.
- [x] Align example agents and authoring docs with explicit mixed `resultMode` usage, including one dedicated text-mode example.
- [x] Add one standalone eval harness example plus docs showing how fixed eval suites should use the `createAiman()` API outside the core runtime.

## Current Status

- Current focus: Final handoff after the agent-authoring documentation rewrite.
- Last completed action: Rewrote `docs/agent-authoring.md` into a clearer step-by-step guide with a quick-start path, explicit `text` vs `schema` guidance, copyable starter patterns, and a simpler workflow, then formatted and re-checked the file.
- Blocked by: none.

## Progress Log

- 13:18 - Rewrote `docs/agent-authoring.md` so it now leads with the practical rules, explains when an agent should exist, makes `text` vs `schema` easier to choose, adds copyable starter patterns, and shifts the guide from reference-style prose to a concrete workflow for creating good agents.
- 13:21 - Ran `bunx prettier --write docs/agent-authoring.md` and `bunx prettier --check docs/agent-authoring.md` to verify the rewritten guide is formatted cleanly.
- 13:02 - Started a documentation-quality pass focused on whether the current docs are easy to understand and actually teach how to create good agents, with the immediate plan to tighten `docs/agent-authoring.md` and any supporting examples or debug guidance that still read like reference material instead of a practical workflow.
- 12:51 - Removed legacy `mode` from `.aiman/agents/hello.md`, added explicit `resultMode: text` plus `Stop Conditions`, and verified the bundled agent with `bun run dev agent check hello --scope project`.
- 12:54 - Rewrote the repo `aiman-profile-authoring` skill and its `agent-design` / `debugging` references from the old `result.json` / `resultType` / `handoff` language to the current `run.json` + `resultMode` + `finalText|structuredResult` model.
- 12:56 - Resolved the foreground `aiman run` contract drift by keeping the live text-mode `finalText` behavior and updating durable memory/runtime/CLI docs to describe that behavior consistently.
- 12:42 - Reviewed the finished contract refactor as a production-readiness check and found three alignment gaps: a bundled repo agent still uses legacy `mode`, the legacy `aiman-profile-authoring` skill still teaches `result.json` / `handoff`, and foreground `aiman run` behavior/docs disagree about whether success should print `summary` or `finalText`.
- 09:08 - Started the v1 runtime-contract shift after aligning on the article-driven direction: provider-native skills, file-first outputs, and a thin runtime ledger.
- 09:14 - Decided to implement this in one forward-only pass by adding `resultMode`, defaulting agents to `text`, keeping an explicit `schema` mode for structured runs, and renaming the canonical persisted run record to `run.json`.
- 09:42 - Finished the runtime/result-mode migration in the TypeScript code: agents now default to `text`, schema-mode prompts append the JSON contract, and persisted runs now store `finalText` or `structuredResult` in `run.json`.
- 09:55 - Updated the fixture layer and focused test suites so the default path exercises text-mode agents while schema-mode coverage stays explicit where structured results are required.
- 10:07 - Promoted the new contract into `MEMORY.md`, `ARCHITECTURE.md`, and the runtime/authoring/debugging CLI docs so the repo now documents `run.json` as a thin ledger and file-first outputs as the preferred pattern.
- 10:26 - Renamed the shared provider helper module to `src/lib/providers/runtime.ts`, renamed old contract-heavy helpers such as `buildPrompt` and `finalizeCompletedRun`, renamed `src/lib/run-store.ts` to `src/lib/run-records.ts`, and changed terminal run output from `runPath` to `runFile` for clearer naming.
- 10:33 - Removed the unused shared `runCommandCapture` helper, made schema-only parsing helpers internal, and re-verified the runtime with typecheck, lint, and the focused provider/runs/API/CLI/TUI test suites.
- 10:36 - Reviewed the authored-agent setup against the Claude Managed Agents references. Conclusion: the repo is directionally strong on narrow agent definitions, shared bootstrap context, and explicit result contracts, but it still has contract drift around legacy `mode` acceptance and no first-class per-agent capability contract for tools, skills, or resources.
- 10:49 - Narrowed the follow-up to one simplification pass: reject legacy authored-agent `mode` outright, stop carrying legacy `mode` through user-facing run types, and update operator-facing docs/tests before deciding whether a lightweight capability declaration is worth adding later.
- 10:58 - Finished the contract cleanup: `mode` is now rejected explicitly in authored agents, user-facing run results no longer carry stale mode metadata, and `agent show` / `agent check` now surface `resultMode` directly.
- 11:02 - Added `docs/claude-managed-agents-concepts.md` to capture the practical comparison: keep agent/runtime separation, consider descriptive capability visibility next, and avoid recreating hosted environment/session abstractions inside the CLI.
- 11:03 - Re-ran `bun test test/cli.test.ts test/provider-adapters.test.ts`, `bun run typecheck`, and `bun run lint` successfully after the cleanup and comparison-note pass.
- 11:18 - Expanded `.agents/skills/claude-managed-agents/SKILL.md` so the skill now explicitly covers Managed Agents architecture plus prompt engineering, prompting tools, testing/evals, and guardrail harness patterns.
- 11:22 - Added focused reference modules for prompting overview, prompting best practices, prompting tools, testing/evals, hallucination reduction, consistency, jailbreak mitigation, and prompt-leak reduction, each ending with direct `aiman` guidance about whether the change belongs in authored prompts, shared context, harnesses, or the thin runtime.
- 11:24 - Converted `references/prompt-engineering.md` into a short bridge module to reduce overlap, then validated that the router references 19 real modules and that every new module exposes explicit `aiman` mapping.
- 11:42 - Confirmed the current repo already supports API-based harness scripts via `createAiman()`, that the current example agents describe schema-style outputs without declaring `resultMode`, and that `agent show` currently exposes provider rights but no authored capability summary.
- 12:06 - Added optional informational `capabilities` to the authored-agent contract and API/CLI surfaces, persisted capabilities into the run launch snapshot, and surfaced them in `agent show`, `agent check`, and run inspection without changing runtime behavior.
- 12:11 - Tightened authored prompt defaults by adding `Stop Conditions` to the built-in/scaffolded prompt shape and making `agent check` warn when that section is missing.
- 12:17 - Updated the example agents and authoring/runtime/CLI docs so schema-shaped examples now declare `resultMode: schema`, added one dedicated text-mode example, and documented the new `capabilities` and eval-harness patterns.
- 12:21 - Added `examples/eval-harness.ts` plus `examples/eval-suite.sample.json` and `docs/agent-evals.md` as the first fixed-suite harness example for `createAiman()`-based agent evaluation outside the core runtime.
- 12:26 - Re-ran `bun run typecheck`, `bun test test/cli.test.ts test/api.test.ts test/provider-adapters.test.ts test/runs.test.ts`, `bun run lint`, and a usage-level invocation of `bun run examples/eval-harness.ts`.
