# Daily Memory Log - 2026-04-12

## Tasks

- [x] Fix Codex-backed `aiman` runs so the current Codex CLI does not reject reviewer launches outside a trusted Git directory.
- [x] Update the home `aiman` skill so it can guide cheap, reliable harness creation.
- [x] Add a minimal plan-execution harness example that runs builder agents, deterministic checks, conditional review, and final review.

## Current Status

- Current focus: improving harness authoring guidance and adding a harness v1 example.
- Last completed action: Added `--skip-git-repo-check` to Codex provider launches, updated run-rights docs/metadata, and verified with focused tests, typecheck, lint, and touched-file formatting.
- Blocked by: none.

## Progress Log

- 00:00 - Started investigation of reviewer failures that report `Not inside a trusted directory and --skip-git-repo-check was not specified`; confirmed the failure is in Codex CLI launch arguments rather than the authored reviewer prompt.
- 00:00 - Updated `src/lib/providers/codex.ts` so every Codex-backed run includes `--skip-git-repo-check`, then aligned provider capability text, runtime docs, root memory, and affected tests.
- 00:00 - Verified the fix with `bun test test/provider-adapters.test.ts test/ui-workbench.test.tsx` and `bun run typecheck`; both passed.
- 00:00 - Ran `bun run lint`, which completed with one unrelated existing warning in `src/lib/providers/gemini.ts`; repo-wide `bun run format:check` is still blocked by existing formatting issues, but touched-file Prettier check passed.
- 00:00 - Started approved work to improve the `aiman` skill for harness creation and add a minimal plan-execution harness example.
- 00:00 - Updated the home `aiman` skill with agent-vs-harness routing, harness bad smells, refreshed UI metadata, and a new `references/harness-design.md` reference.
- 00:00 - Added `examples/plan-harness.ts`, a sample JSON plan, focused harness tests, and `docs/agent-harnesses.md`; updated `AGENTS.md`, architecture, and root memory to point at the plan harness as the minimal orchestration example.
- 00:00 - Verified harness work with `bun test test/plan-harness.test.ts`, `bun run typecheck`, touched-file Prettier check, `bun run lint`, and full `bun test test/*.ts*`; lint still reports the existing unrelated `GeminiJsonOutput` warning while exiting successfully.
