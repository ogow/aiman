# Daily Memory Log - 2026-03-28

## Tasks

- [x] Simplify the runtime structure by splitting run execution, persistence, and lookup into smaller focused modules.
- [x] Simplify agent loading and CLI output code so the public mental model stays obvious.
- [x] Update architecture/docs to describe the clearer module boundaries for agent management.
- [x] Run verification and record the outcome for the simplification pass.
- [x] Make `aiman run` exit non-zero for unsuccessful runs and add CLI regression coverage.
- [x] Accept CRLF frontmatter in agent Markdown files and add CLI coverage.
- [x] Restore timeout kill escalation in `src/lib/runs.ts` and add stubborn-run coverage.
- [x] Flatten the CLI surface to top-level `list`, `show`, `run`, and `inspect` commands.
- [x] Remove the old `agent ...` and `runs ...` public command structure from the code and docs.
- [x] Update CLI coverage and help-text expectations for the simplified command model.
- [x] Reframe `docs/agent-runtime.md` around `aiman` as an agent manager/runtime for external parent agents.
- [x] Update `ARCHITECTURE.md` to remove built-in supervisor language and describe the external-parent model.
- [x] Update any narrower docs that still imply `aiman` is the main orchestrator.
- [x] Fix run ID collisions in `src/lib/runs.ts`.
- [x] Handle provider spawn failures cleanly in `src/lib/runs.ts`.
- [x] Make agent lookup resolve listed frontmatter names in `src/lib/agents.ts`.
- [x] Add regression coverage for the run ID, spawn failure, and agent lookup fixes.
- [x] Add `MEMORY.md` with the core agent operating rules and task-writing rules.
- [x] Move architecture guidance to a root `ARCHITECTURE.md` file and update routing docs to use it.
- [x] Remove the `memory.ts` utility and test coverage that incorrectly moved memory behavior into runtime code.
- [x] Add a repo skill that instructs agents to review and update memory/docs after meaningful actions and architecture/code/logic decisions.
- [x] Run verification and record the outcome.
- [x] Research reliable CLI-wrapped multi-agent patterns and capture a v1 runtime design for `aiman`.
- [x] Replace the scaffold command surface with `agent`, `run`, and `runs` commands.
- [x] Implement Markdown agent loading and validation for `.aiman/agents/`.
- [x] Implement strict headless `codex` and `gemini` provider adapters.
- [x] Implement local run execution and persistence under `.aiman/runs/`.
- [x] Add fixture-based CLI and adapter coverage for the v1 runtime.
- [x] Update memory rules to require splitting larger plans into smaller daily-memory tasks before implementation.

## Current Status

- Current focus: none
- Last completed action: Simplified the runtime and agent-loading structure, updated architecture/runtime docs, and re-ran formatting, lint, typecheck, tests, and build successfully.
- Blocked by: none

## Progress Log

- 11:34 - Reviewed the current repo structure, existing `.agents/memories` files, and deleted historical docs to align the implementation with the current codebase.
- 11:36 - Added `MEMORY.md`, `AGENTS.md`, focused docs under `docs/`, today's daily memory file, and `src/lib/memory.ts` with coverage in `test/memory.test.ts`.
- 11:36 - Verified the implementation with `typecheck`, `test`, `build`, and `lint`; all checks passed.
- 12:03 - Re-scoped the implementation after clarifying that `ARCHITECTURE.md` must live at the repo root and documentation/memory maintenance should be driven by a skill, not TypeScript runtime code.
- 12:04 - Moved architecture guidance to root `ARCHITECTURE.md`, removed `src/lib/memory.ts` and `test/memory.test.ts`, and added `.agents/skills/memory-maintainer/SKILL.md`.
- 12:04 - Re-ran `typecheck`, `test`, `build`, and `lint` after the correction; all checks passed.
- 14:42 - Researched current multi-agent reliability guidance and provider tooling, then added `docs/agent-runtime.md` to define a simple `aiman` v1 built around a supervisor, thin CLI adapters, structured handoffs, and repo-local run storage.
- 17:25 - Broke the approved v1 runtime plan into smaller implementation tasks in daily memory, but only after starting the work; this needs to be corrected as a standing rule.
- 18:11 - Replaced the scaffold commands with the v1 `agent`, `run`, and `runs` CLI; added Markdown agent loading, strict `codex` and `gemini` adapters, slim run results, persisted run records, and fixture-based CLI/adapter coverage without generated mock binaries.
- 18:12 - Verified the implementation with `npm run typecheck`, `npm test`, `npm run lint`, and `npm run build`; all checks passed.
- 18:26 - Updated `MEMORY.md`, `AGENTS.md`, `docs/memory.md`, and the memory-maintainer skill so larger approved plans must be split into smaller daily-memory tasks before implementation begins.
- 18:55 - Fixed the review findings in the v1 runtime: run IDs now include a random suffix, spawn failures are persisted as normal error runs instead of crashing with an unhandled child-process error, and agent lookups fall back to the frontmatter name exposed by `agent list`.
- 18:56 - Added regression coverage in `test/runs.test.ts` and `test/cli.test.ts`, then re-ran `npm run typecheck`, `npm test`, `npm run lint`, and `npm run build`; all checks passed.
- 19:08 - Rewrote `docs/agent-runtime.md`, updated `ARCHITECTURE.md`, and added a note to `docs/cli.md` so the repo now describes `aiman` as a local specialist-agent manager/runtime called by external parent agents such as Codex, Gemini, or Claude Code.
- 19:24 - Flattened the CLI from `agent ...` / `runs ...` namespaces to top-level `list`, `show`, `run`, and `inspect` commands; removed the old command modules and updated the docs to match the simpler mental model.
- 19:25 - Re-ran `npm run typecheck`, `npm test`, `npm run lint`, and `npm run build` after the CLI simplification; all checks passed.
- 19:47 - Fixed the review findings in the flattened runtime: `aiman run` now exits non-zero for unsuccessful runs, frontmatter parsing accepts CRLF line endings, and timed-out runs escalate from `SIGTERM` to `SIGKILL` after a short grace period.
- 19:48 - Added CLI and runner regression coverage for failed-run exit codes, CRLF agent files, and stubborn timed-out providers; re-ran `prettier --check` on changed files plus `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`, all successfully.
- 19:49 - Ran `npm run check`; it still fails for pre-existing prettier issues under `.agents/skills/yargs/references/` plus existing formatting drift in `src/lib/agents.ts`, not from this fix set.
- 20:14 - Simplified the runtime by moving run-path, run-state, persisted-result, and inspection helpers into `src/lib/run-store.ts`, leaving `src/lib/runs.ts` focused on orchestrating one specialist execution.
- 20:16 - Simplified agent lookup in `src/lib/agents.ts` by reading the small local agent catalog once and resolving by file id or frontmatter name, instead of branching through nested `ENOENT` fallback logic.
- 20:18 - Updated `ARCHITECTURE.md` and `docs/agent-runtime.md` to describe the clearer module boundaries and the smaller provider contract.
- 20:23 - Re-ran `prettier --write` on changed files plus `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; all checks passed.

## Decisions

- 11:34 - Implement the memory plan as repo scaffolding plus reusable utility/test coverage, because the current repo no longer has the old runtime prompt-assembly layer.
- 11:36 - Keep daily memory out of the auto-loaded context and expose it through explicit lookup helpers instead, so the always-on context stays small.
- 12:03 - Treat memory/document maintenance as agent workflow guidance in a repo skill rather than as executable application logic.
- 12:04 - Keep architecture guidance in root `ARCHITECTURE.md`; reserve `docs/` for narrower project-specific material.
- 14:42 - Keep `aiman` v1 local and simple: wrap existing CLIs through provider adapters, route agent-to-agent collaboration through structured handoffs, and avoid peer-to-peer free-form messaging.
- 18:11 - Keep the parent-facing run output intentionally slim and persist detailed execution metadata under `.aiman/runs/` for later inspection.
- 18:11 - Keep provider tests fixture-based and parser-focused rather than creating fake provider executables during test runs.
- 18:26 - Treat plan decomposition as a required memory step, not just a nice-to-have; larger approved work must be broken into smaller daily-memory tasks before implementation starts.
- 18:55 - Treat provider spawn failures as ordinary persisted error runs so `aiman run` does not crash or leave truncated run state when the downstream process cannot start.
- 18:55 - Allow agent lookup by frontmatter `name` as a fallback because that is the identifier surfaced by `agent list` and used by the CLI surface.
- 19:08 - Keep `aiman` out of the parent-agent role; Codex, Gemini, Claude Code, or another external caller should choose specialists and own the wider workflow, while `aiman` owns specialist execution and run persistence.
- 19:24 - Keep the public CLI flat unless a nested namespace buys real clarity; `aiman` is easier to understand with four top-level verbs than with separate `agent` and `runs` trees.
- 19:47 - Treat timed-out runs as errors, not cancellations, and escalate to `SIGKILL` after a short grace period so stubborn provider processes cannot hang `aiman run` indefinitely.
- 20:14 - Keep the runtime split into small boring pieces: `agents.ts` for the agent catalog, `runs.ts` for orchestration, and `run-store.ts` for persisted run files and external result shaping.
- 20:16 - Prefer reading the small local agent catalog directly over ENOENT-driven fallback branches; the repo is small enough that the simpler control flow is the better tradeoff.
- 20:16 - Avoid keeping extra runtime APIs until the CLI or tests actually need them.

## Carry Forward

- Keep the daily-file system simple in v1 and avoid introducing extra task or decision directories.
- Build `aiman` around a narrow cross-provider runtime contract and avoid inventing a universal prompt format.
- If richer orchestration is added later, keep it additive on top of the current top-level `list` / `show` / `run` / `inspect` surface instead of overloading the base execution path.
