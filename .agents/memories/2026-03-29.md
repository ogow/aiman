# Daily Memory Log - 2026-03-29

## Tasks

- [x] Add file-first run report support with `report.md`, artifact directory paths, and provider handoff environment variables.
- [x] Extend run persistence and inspection to surface parsed report frontmatter and referenced artifacts without adding task or memory runtime concepts.
- [x] Add tests for report parsing, inspection output, and run-time report/artifact handoff behavior.
- [x] Enforce the TypeScript module-style bans in lint so agents cannot accidentally reintroduce them.
- [x] Update the lint scripts and TypeScript style docs to point at the enforced rules.
- [x] Run verification and record the outcome for the lint-enforcement pass.
- [x] Normalize Node built-in imports to match the repo TypeScript style rules.
- [x] Extract inline runtime/provider contract object shapes into named TypeScript types.
- [x] Clean up small style inconsistencies found in the review pass.
- [x] Run verification and record the outcome for the style cleanup pass.
- [x] Add a repo TypeScript style guide based on the Google TypeScript Style Guide.
- [x] Update agent-facing docs so agents consult the TypeScript style guide when editing `.ts` files.
- [x] Record the new style decision in durable memory/docs.
- [x] Format changed docs and record the outcome.

## Current Status

- Current focus: none
- Last completed action: Added file-first `report.md` handoff support, surfaced parsed reports and referenced artifacts through `inspect`, updated docs, and verified lint, typecheck, tests, and build successfully.
- Blocked by: none

## Progress Log

- 19:18 - Reviewed the current run/inspect flow, provider adapter contracts, frontmatter parser, tests, and worktree state to map the file-first `report.md` feature onto the existing thin specialist-runner architecture.
- 19:20 - Chose a separate report parser and no new task/memory runtime surface so `aiman` stays a file-first execution substrate and the main agent keeps orchestration responsibilities.
- 19:31 - Added canonical run-level `report.md` and `artifacts/` paths, passed them into downstream providers through prompt/footer and allowlisted environment variables, and taught runs to surface `reportPath` when a report is present.
- 19:37 - Added `src/lib/report.ts` to parse Markdown reports with YAML-style frontmatter, preserve the body, resolve artifact references, and surface parse errors without failing the run.
- 19:43 - Extended `aiman inspect` to include parsed report metadata and artifact references, updated fixture data and provider/run tests, and documented the file-first specialist handoff model in architecture/runtime docs.
- 19:47 - Ran `npm run lint && npm run typecheck && npm test && npm run build`; all checks passed for the file-first run report feature.
- 09:05 - Reviewed the Google TypeScript Style Guide, `AGENTS.md`, `MEMORY.md`, the latest daily memory, and the current repo tooling to decide how to add durable TypeScript style rules for agents.
- 09:12 - Added `docs/typescript-style.md` as a small repo-specific ruleset adapted from the Google TypeScript Style Guide, with emphasis on ES modules, named exports, relative imports, `import type`, small modules, and readability.
- 09:13 - Updated `AGENTS.md` so agents read the TypeScript rules when editing `.ts` files, and updated `MEMORY.md` so the rule becomes a durable cross-session project truth.
- 09:14 - Ran `prettier --write` on the changed Markdown files; formatting completed successfully.
- 09:22 - Reviewed the current code against the new TypeScript rules and identified three cleanup areas: Node built-in default imports, large inline exported contract types, and one remaining inline `ENOENT` helper check.
- 09:36 - Normalized Node built-in imports across `src/` and `test/` to namespace imports, matching the repo TypeScript rules adapted from the Google guide.
- 09:39 - Extracted the exported runtime/provider contract shapes in `src/lib/types.ts` into named types such as `RunStatus`, `RunPaths`, `UsageStats`, `PreparedRunInput`, and `CompletedRunInput`.
- 09:40 - Reused the shared `hasErrorCode` helper in `src/lib/providers/shared.ts` and aligned shared helper signatures with the new named types.
- 09:43 - Re-ran `prettier --write` on changed files plus `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; all checks passed after a small follow-up fix to include `runDir` in `buildRunPaths`.
- 09:50 - Confirmed `oxlint` can enforce the intended bans with a repo config: `import/no-default-export`, `@typescript-eslint/no-namespace`, `@typescript-eslint/no-require-imports`, and `@typescript-eslint/triple-slash-reference`. Verified the config against temporary violating files before applying it to the repo.
- 09:56 - Added a minimal `.oxlintrc.json` that enables the `import` plugin alongside the existing TypeScript plugins and bans default exports, namespaces, `require`-style imports, and triple-slash references.
- 09:57 - Updated `package.json` so both `npm run lint` and `lint-staged` use the repo `oxlint` config explicitly, and updated `docs/typescript-style.md` to document the enforced subset.
- 09:59 - Re-ran `prettier --write` on the changed config/docs files plus `npm run lint`, `npm run typecheck`, `npm test`, and `npm run build`; all checks passed.

## Decisions

- 19:20 - Keep `aiman` file-first: standardize `report.md` plus artifacts inside each run directory, and avoid adding SQLite, task commands, or memory storage to the platform.
- 19:31 - Treat `result.json` as the slim runtime envelope and `report.md` as the optional specialist-to-parent handoff contract; the external parent agent stays responsible for task state and memory.
- 09:05 - Adapt the Google guide into a short repo-specific rules doc instead of trying to copy the whole guide verbatim; agents need a practical subset that matches this project and current tooling.
- 09:12 - Treat the Google guide as a source for durable repo rules, not as a literal formatting spec; Prettier remains the formatter and the repo-specific rules file is the contract agents should follow.
- 09:22 - Apply the style review as a focused cleanup pass instead of broad churn: normalize imports, name the exported contract types, and fix only the small inconsistencies that directly support the new rules.
- 09:39 - Prefer named shared types over repeated inline object literals in exported runtime contracts; it makes provider adapters and persistence code easier to scan and reuse.
- 09:50 - Keep the lint enforcement narrow and intentional: ban the exact TypeScript/module constructs the repo has decided against, rather than enabling a large style ruleset all at once.
- 09:56 - Keep the repo `oxlint` config minimal and explicit; enforce only the TypeScript/module bans that represent durable project decisions, and leave broader style guidance in docs until the team wants stricter automation.

## Carry Forward

- Keep TypeScript style rules explicit and small enough that agents can follow them reliably.
- Keep orchestration concerns outside `aiman`; prefer file-first run handoff through `report.md` plus `artifacts/` when specialists need to return structured follow-up context.
